## 1. Parity pins (write first — they define "output-preserving")

- [x] 1.1 Extend the fixture of `TestBuildStorage_PlanIsOutputPreserving` in `pkg/build/build_storage_plan_test.go` to cover every controller kind — a StatefulSet-owned pod, a DaemonSet-owned pod, a Deployment-owned pod (via a `kube_replicaset_owner` row), a bare-ReplicaSet-owned pod, a Job-owned pod whose Job carries its own tracking-id, a Job-owned pod under an annotated CronJob (Job unannotated, `kube_job_owner` row present), an ownerless pod — plus an unscheduled pod (no `node` label), a `node=` root naming a Kubernetes node no loaded pod runs on, and a same-named StatefulSet in another namespace carrying a different tracking-id; build once through `fullPlan` and once through `storagePlan(roots)`, serialise both, strip `containers` from the first, assert byte equality. Verify it passes today (the fixture is still read unrestricted) so the pin is green before group 3 moves anything.
- [x] 1.2 Add a `MockQuerier` matcher helper in `pkg/build` tests (beside the existing scoped-query capture used by `podscope_test.go`) that records, per family, the rendered alternation values of every issued query, so later tasks can assert "restricted to exactly {…}" rather than string-matching whole queries. Verify a unit test round-trips `pod=~"a|b"` and `job_name="x"` into `[]string{"a","b"}` / `[]string{"x"}`.

## 2. `pkg/promql`: twelve more scopeable families, fixed selectors shared

- [x] 2.1 Extract every fixed selector from `Render`'s `switch` arms into one `fixedSelector map[Query]string` in `pkg/promql/queries.go` (`type=~"ExternalIP|InternalIP"`, `condition="Ready"`, `jobOwnerCronJobSelector`, `argoTrackingIDPresentSelector` for the six annotation families, the service-graph sentinel and link matchers), and make `Render` read `braces(fixedSelector[q])` (design D2). Verify `TestRender_EmptySelectorMatchesBaseline` passes with `render-baseline.txt` byte-identical (`git diff --exit-code pkg/promql/testdata/render-baseline.txt`).
- [x] 2.2 Grow `scopedLabel` in `pkg/promql/scope.go` to the table of design D1 (four node families on `node`; `kube_replicaset_owner` / `kube_replicaset_annotations` on `replicaset`; `kube_job_owner` / `kube_job_annotations` on `job_name`; `kube_deployment_annotations` on `deployment`; `kube_statefulset_annotations` on `statefulset`; `kube_daemonset_annotations` on `daemonset`; `kube_cronjob_annotations` on `cronjob`), export `NodeScopedQueries`, `ControllerScopedQueries` and `ReferenceScopedQueries` (the union with `PodScopedQueries`), and make `RenderScoped` render `{<fixed>,<request>,<scope>}` omitting empty parts. Verify `TestRenderScoped` gains cases: `kube_job_owner` with an empty selector renders `last_over_time(kube_job_owner{owner_kind="CronJob",owner_is_controller="true",job_name=~"a|b"}[5m])`; `kube_node_status_addresses` keeps its `type` alternation ahead of `node`; `kube_deployment_annotations` keeps `annotation_argocd_argoproj_io_tracking_id!=""`; a non-scopeable query still returns `ok=false`.
- [x] 2.3 Add `TestRenderScoped_IsRenderPlusOneMatcher` in `pkg/promql/scope_test.go`: for EVERY key of `scopedLabel`, with and without an az/env/cluster/namespace selector, the scoped string equals `Render`'s output with exactly one `<label>=~"…"` (or `<label>="…"`) matcher appended as the last element inside the braces. Verify it passes and fails (mutation-checked) when one family's fixed selector is dropped from the table.
- [x] 2.4 Add `TestQueryDims_ReferenceScopedLegsUnchanged` asserting every `ReferenceScopedQueries` member keeps its `queryDims` entry (`dimsNamespaced` for pods and controllers, `dimsClusterScoped` for the node four). Verify it passes and that `TestQueryDims_EveryQueryListed` still passes.

## 3. `pkg/build`: plan, tally and the generic scoped reader

- [x] 3.1 In `pkg/build/topologyplan.go` rename `topologyPlan.scopePods` to `byReference`, add `nodeRoots []string` (sorted `StorageRoots.Nodes`), fill both in `storagePlan(roots)`, and make `issuesFirstWave` return false for every `promql.ReferenceScopedQueries` member under a by-reference plan (design D4). Verify `go build ./...` succeeds and `TestReadTopology_FanOutLegCount` still pins 37 / 43 for `fullPlan`.
- [x] 3.2 Replace `topologyVectors.QoSScopeIssued` / `PodScopeIssued` with `ScopeIssued map[promql.Query]bool` (initialised in `readTopology` before any wave starts; each wave writes only its own families' keys), and make `tallySeries` report a by-reference or QoS family iff its key is set (design D5). Verify the existing `TestBuildStorage_FanOutLegCount` empty-scope case still shows no `kube_pod_info` key and `go vet ./pkg/build/` is clean (no concurrent map write: run `go test -race ./pkg/build/`).
- [x] 3.3 Create `pkg/build/scopedread.go` with `legMode` (`required`, `optional`, `optionalTracking(*bool)`) and `issueScoped(ctx, callerCtx, q, window, end, opts, sel, target, scope, mode) error` extracted from `readScopedPods` (design D3): empty scope ⇒ return nil without issuing or flagging; chunk under `opts.qosScopeBatchBytes()`; issue under `scopeConcurrency` via `promql.RenderScoped`; pre-sized slots merged in chunk order; `required` returns the first chunk error; `optional` logs `optional topology query failed` and skips the chunk; `optionalTracking` additionally sets the flag; `optionalQueryFatal(callerCtx, err)` fails every mode on caller cancellation; set `v.ScopeIssued[q]` when at least one chunk was issued. Rewrite `readScopedPods` as two `issueScoped(..., required)` calls. Verify every existing `TestReadScopedPods_*` passes unchanged.
- [x] 3.4 Add `pkg/build/scopedread_test.go`: `TestIssueScoped_OptionalChunkDegradesAlone` (three chunks, middle fails ⇒ nil error, two chunks' series merged in order, family flagged issued), `TestIssueScoped_OptionalTrackingSetsFlag`, `TestIssueScoped_RequiredChunkFails`, `TestIssueScoped_EmptyScopeIssuesNothing` (no query, no flag), `TestIssueScoped_CallerCancellationFailsOptional`. Verify all pass and the degrade test fails when the chunk error is returned instead of logged.

## 4. Node wave

- [x] 4.1 Wrap the pod wave's launch in `readTopology` with `signalWhenDone(..., podsDone)` so `podsDone` closes on every return path (including the empty-scope early return and a chunk error). Verify a unit test that fails the pod wave still sees `podsDone` closed (no goroutine leak under `go test -race`).
- [x] 4.2 Create `pkg/build/nodescope.go` with `nodeScope(pods model.Vector, roots []string) []string` (pure: sorted, de-duplicated union of non-empty `node` labels and roots) and `readScopedNodes(ctx, callerCtx, q, window, end, opts, sel, roots, v, podsDone)` issuing the four node families as `required` via `issueScoped` (design D3 wave 2). Verify `TestNodeScope` covers: unscheduled pod contributes nothing; duplicate node names collapse; roots alone yield a scope; empty pods + no roots ⇒ empty.
- [x] 4.3 Add `pkg/build/nodescope_test.go`: `TestReadScopedNodes_RestrictedToPodNodesAndRoots` (spec scenario "Node read is restricted to the pods' nodes and node roots" — every node query carries `node=~"n1|n2|n9"` plus its fixed selector and the request matchers), `TestReadScopedNodes_RootWithoutPodsIsLoaded` (spec scenario "Kubernetes node root with no mounting pod is still drawn" — no bindings, `node=n9` ⇒ four node queries restricted to `{n9}`, no pod query), `TestReadScopedNodes_EmptyScopeIssuesNothing`, `TestReadScopedNodes_ChunkFailureFailsBuild`. Verify all pass.

## 5. Controller wave

- [x] 5.1 Create `pkg/build/controllerscope.go` with the pure scope functions: `controllerScope(podOwner model.Vector) map[string][]string` (rows with `owner_is_controller="true"` and non-empty `owner_name`, grouped by `owner_kind`, each list sorted and de-duplicated), `deploymentScope(direct []string, rsOwner model.Vector) []string` (direct ∪ `owner_name` of rows with `owner_kind="Deployment"` — the same filter `resolvePodOwners` uses), `cronJobScope(direct []string, jobOwner model.Vector) []string` (direct ∪ every row's `owner_name`). Verify `TestControllerScope*` covers: non-controller rows ignored; empty names ignored; kinds outside the annotation table (`ReplicationController`, `Node`, a CRD kind) grouped but unused by the wave; determinism across input order.
- [x] 5.2 Add `readScopedControllers(ctx, callerCtx, q, window, end, opts, sel, v, podsDone)` (design D3 wave 3): wait on `podsDone`; stage A issues, under one bounded sub-errgroup, `kube_replicaset_owner` (required) + `kube_replicaset_annotations` (optional) on the ReplicaSet names, `kube_job_owner` (required) + `kube_job_annotations` (optionalTracking → `v.JobAnnotationsDegraded`) on the Job names, `kube_statefulset_annotations` / `kube_daemonset_annotations` (required) on theirs; stage B, after the sub-group returns, issues `kube_deployment_annotations` on `deploymentScope` and `kube_cronjob_annotations` on `cronJobScope` (required). A kind with no names issues nothing. Verify `go vet ./pkg/build/` and `go test -race ./pkg/build/` are clean.
- [x] 5.3 Add `pkg/build/controllerscope_test.go`: `TestReadScopedControllers_RestrictedToLoadedOwners` (spec scenario "Controller read is restricted to the loaded pods' owners" — StatefulSet `orders` + ReplicaSet `web-7d9f` → Deployment `web`; assert the exact alternations, the fixed selectors, that `kube_deployment_annotations` is issued only after `kube_replicaset_owner` returned, and that no Job / DaemonSet / CronJob query is issued), `TestReadScopedControllers_JobResolvesThroughCronJobInTwoStages` (spec scenario "Job-owned mounting pod resolves its CronJob Application through two stages"), `TestReadScopedControllers_KindWithNoOwnersIssuesNothing` (spec scenario "A kind no loaded pod is owned by issues no query" — tally keys absent), `TestReadScopedControllers_RequiredChunkFailsBuild` (spec scenario "A required controller chunk failure fails the build"), `TestReadScopedControllers_JobAnnotationsChunkDegradesAndSuppressesHop` (spec scenario "A degrading controller chunk degrades and suppresses the hop" — build succeeds, `JobAnnotationsDegraded` set, Job-owned pods carry no Application), `TestReadScopedControllers_AccumulatedJobHistoryNotRead` (spec scenario "Accumulated Job history does not reach the storage build" — the mock returns an error for any `kube_job_owner` query WITHOUT a `job_name` matcher, and two series for the restricted one). Verify all pass.

## 6. Wiring, fan-out pins and component fixtures

- [x] 6.1 Launch `readScopedNodes` and `readScopedControllers` from `readTopology` when `plan.byReference` is set, both gated on `podsDone`; leave `fullPlan` launching neither. Verify `TestReadTopology_FanOutLegCount` (37 / 43) passes unchanged and `TestBuildStorage_IssuesNoServiceGraphOrProbeQuery` still passes.
- [x] 6.2 Rewrite `TestBuildStorage_FanOutLegCount` in `pkg/build/build_storage_plan_test.go` to the table of design D6: empty scope, no roots → 18; `node=n9` root alone → 22; one StatefulSet-owned scheduled pod → 25; one Deployment-owned pod via ReplicaSet → 27; one CronJob-owned pod via Job → 27; every kind present → 32, and 38 with one matched volume. Verify each count fails when one by-reference family is put back into the storage first wave (mutation-checked on one family).
- [x] 6.3 Add `TestBuildStorage_ControllerScopeCrossNamespaceCollisionIsHarmless` beside `TestBuildStorage_CrossNamespaceNameCollisionIsHarmless` (spec scenario "Controller scope cross-namespace collision is harmless"). Verify the body equals the unrestricted body.
- [x] 6.4 Run parity pin 1.1 against the by-reference plan; verify byte equality holds, and that removing any one family from `ReferenceScopedQueries` (so it is read unrestricted) also leaves it green — the pin is body equality, not query shape.
- [x] 6.5 Update `internal/api/storage_graph_test.go` mock expectations for the storage plan (the twelve families now appear only as scoped queries, none with an empty scope) and extend `TestStorageGraph_SelectorQueriesCaptured` to assert a `pod=`-rooted request issues `kube_statefulset_annotations` with both `namespace=…` and `statefulset=~…`. Run `go test ./internal/api/ -run 'TestGolden_StorageGraphResponses|TestStorageGraph'` WITHOUT `-update`; verify `git status` shows no change under `internal/api/testdata/golden/`.
- [x] 6.6 Update the `RawSeriesCount` doc comment in `pkg/build/topology.go` and the `topologyLegs` comment block in `topologyplan.go` (which legs are second-wave under which plan; "absent means never read or empty scope"). Verify `go vet ./pkg/build/` is clean.

## 7. Documentation

- [x] 7.1 `docs/upstream-metrics.md`: rewrite the "Fan-out per `/v1/storage-graph` request" section — 18 first-wave legs, the three by-reference waves and their gates (bindings → pods → {nodes, controllers stage A} → controllers stage B), the per-kind formula and the D6 pin table, the empty-scope and chunk rules per family, the error class per family; add the D8 table of families that stay unrestricted with the reason for each. Verify the per-family counts sum to 18 and the pin numbers match 6.2.
- [x] 7.2 `docs/BREAKING.md`: add a non-breaking entry "Storage build reads Kubernetes nodes and controllers by reference" stating that queries change and bodies do not, that `RawSeriesCount` for the twelve families now counts matched-in-scope series and is absent when the scope was empty, and that a `node=` root is now an input to the node read. Verify it names the spec requirement it modifies.
- [x] 7.3 `README.md` and `README.zh-tw.md`: update the storage-graph section's one sentence on what the build reads by reference (pods, their nodes, their controllers) and the leg counts. Verify both READMEs state the same counts.
- [x] 7.4 `CLAUDE.md`: update the `/v1/storage-graph` lifecycle block (18 first-wave legs; three by-reference waves; the D6 pins replace "32/38 with a pod scope, 30/36 without"), the storage-join bullet's test list (add `scopedread_test.go`, `nodescope_test.go`, `controllerscope_test.go`), and the "Storage build reads only what it draws" wording wherever it appears. Verify the stated totals agree with 6.2 and 7.1.
- [x] 7.5 Update the `--netapp-qos-scope-batch-bytes` help text in `internal/config/config.go` and the `Options.QoSScopeBatchBytes` doc comment to say it bounds every data-derived alternation (QoS `volume`; storage `pod`, `node`, controller names). Verify `go vet ./internal/config/` passes.

## 8. Verification gate

- [x] 8.1 Run `make vet` and `make lint`; verify both report clean.
- [x] 8.2 Run `make test`; verify the full suite passes with `-race -shuffle=on` and `git status` shows no golden or `render-baseline.txt` change.
- [x] 8.3 Run `go test ./internal/integration/ -run 'TestGraphSuite'` with Docker available; verify the storage suite passes against real VictoriaMetrics, including a `node=` root on a pod-less node being drawn and a Job-under-CronJob mounting pod carrying the CronJob's Application.
- [x] 8.4 Run `make check-docs`; verify no OpenAPI drift.
- [x] 8.5 Confirm `harden-topology-read-cardinality` has been synced or archived (`grep -c "Storage build reads only what it draws" openspec/specs/storage-graph-api/spec.md` returns 1), then run `openspec validate "scope-controller-legs-by-reference"`; verify it reports no archive-time refusal.
