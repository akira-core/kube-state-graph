## 1. Query layer — new legs, dimensions, families

- [x] 1.1 Verify the four Harvest `system_node` counter names (`node_cpu_busy`, `node_total_ops`, `node_total_latency`, `node_total_data`) and the `node_labels` label set (`model`, `serial`, `version`, `vendor`, `location`) against the Harvest metric catalogue for the versions in `docs/netapp-harvest-preconditions.md`; verify by recording the catalogue reference (template file + version) in that doc's Harvest-series table before any query constant is committed.
- [x] 1.2 Add `QNetAppNodeLabels`, `QNetAppNodeCPUBusy`, `QNetAppNodeTotalOps`, `QNetAppNodeTotalLatency`, `QNetAppNodeTotalData` to `pkg/promql/queries.go` rendered as `last_over_time(<name>[w])`, classified `FamilyHarvest` / `dimsHarvest`; verify `TestQueryFamily_EveryQueryListed` and the `queryDims` exhaustiveness test pass and `TestRender_EmptySelectorMatchesBaseline` passes after regenerating `pkg/promql/testdata/render-baseline.txt` with the five new lines.
- [x] 1.3 Add `QAlerts = "ALERTS"` with the fixed selector `alertstate="firing"` rendered first, a new `dimsAlerts = dimAZ | dimEnv | dimNamespace` constant, and `queryFamily[QAlerts] = FamilyAlerts`; verify a `pkg/promql` render test pins `last_over_time(ALERTS{alertstate="firing",az="zone-a",env="prod",namespace="shop"}[w])` for the four-dimension selector (no `cluster`) and `FamilyAlerts.AcceptsAZ()` is true.
- [x] 1.4 Add `FamilyAlerts` to `promql.Families` and `ParseFamily`, split table validation into `requiredFamilies` (the original five) so an unserved `alerts` is accepted, and make `SingleBackendTable` serve all six; verify `pkg/promql/backend_test.go` covers "optional family left unserved accepted", "family left unserved rejected" still fails for `harvest`, and `singletable_test.go` asserts six families.
- [x] 1.5 Make `Router.Instant` answer an empty vector with a Debug log (not the zone Warn) when `Table.Select` yields no backend for `FamilyAlerts`, and log one Info at table load when no backend serves it; verify `pkg/promql/router_test.go` asserts the empty result and log level, and `backendsfile` tests accept `families: [alerts]` unchanged.
- [x] 1.6 Exclude `FamilyAlerts` from the `selector_family_empty` Warn in `pkg/build`; verify `selector_warn_test.go` gains a case where an empty `ALERTS` vector under `az`/`env` emits no Warn.

## 2. Graph model

- [x] 2.1 Add `NodeTypeNetAppSVM` and `NetAppSVMNode` (labels `{ontap_cluster}`; every attribute accessor nil / empty) to `pkg/graph/node.go`; verify `node_test.go` asserts the sealed-interface contract and `SortNodes` ordering for the new type.
- [x] 2.2 Add `graph.Hardware{Model, Serial, Version, Vendor, Location}`, `graph.NodePerf{CPUBusyPct, TotalOps, TotalLatencyUs, TotalBytesPerSec *float64}`, `graph.Alert{Name, State, Severity}` and the `Hardware()`, `Perf()`, `Alerts()` accessors on `GraphNode` with `HardwareValue` / `PerfValue` on `NetAppNode` and `AlertsValue` on `PodNode`, `K8sNode`, `PVCNode`, `NetAppNode`, `NetAppAggrNode`; verify `node_test.go` asserts nil / empty on every other type.
- [x] 2.3 Add `EdgeTypeStorageFlow = "storage-flow"` and its `EdgeTypes` registry entry (source `[netapp-node, netapp-aggr, netapp-svm, pvc, pod]`, target `[netapp-aggr, netapp-svm, pvc, pod, node]`, directed, `may_cross_cluster: false`, labels `tier` and `attribution`); verify `registry_test.go` and `internal/api/edge_type_validation_test.go` accept `?edge_type=storage-flow` on `/v1/graph` with a 200 and zero edges.
- [x] 2.4 Add `graph.StorageRoots{ONTAPClusters, Nodes, Aggrs, SVMs map[string]struct{}; Pods map[PodRef]struct{}}`, `graph.StorageScope{Clusters, Namespaces, Roots}` and `NewStorageScope(...)`; verify `scope_test.go` asserts set construction de-duplicates and that `Requested()` per side reports true iff any root parameter for that side was non-empty.

## 3. Topology read — Harvest node legs and inventory

- [x] 3.1 Add the five node legs as `fetchOptional` in `ReadTopology` and thread their vectors into `resolveNetAppStorage`; verify `pkg/build/netapp_test.go`'s fan-out pins move to 36 legs with no matched volume and 42 with one (before the alerts leg), and every leg degrades log-and-continue in `netapp_test.go`.
- [x] 3.2 Build `hardwareByNode` (per-field lexically-smallest non-empty label) and `perfByNode` (verbatim float, lexically-smallest on duplicate) indexes keyed `(oc, node)` and stamp `HardwareValue` / `PerfValue` on every materialised `NetAppNode`; verify unit tests cover the spec scenarios "Hardware attribute resolved" (empty `location` omitted), "Hardware absent", "Performance counters resolved verbatim", "Partial performance counters" and "High CPU does not degrade health".
- [x] 3.3 Collect `Topology.NetAppInventory` in the existing `volume_labels` pass (every `(oc, node, aggr, svm)`; aggregate owner via `pickOwner`) plus node keys from `node_labels` / `node_new_status` / the counters and aggregate keys from `aggr_*`, and expose `svmByPVC` as `Topology.SVMByPVC`; verify a unit test asserts the inventory is a pure function of the vectors (order-free) and that `assemble` / `/v1/graph` output is byte-identical with the inventory populated.

## 4. Alert overlay

- [ ] 4.1 Add the `ALERTS` leg as `fetchOptional` in `ReadTopology` (routed through `FamilyAlerts`) and record its raw count; verify the fan-out pins move to 37 / 43 and `netapp_test.go` + `topology_test.go` still pass.
- [ ] 4.2 Implement `pkg/build/alerts.go`: `alertIndex` built from the assembled node set (pods / PVCs by `(identity, ns, name)` and `(ns, name)`; K8s nodes by `(identity, name)` and `name`; controllers by `(oc, node)` and `node`; aggregates by `(oc, aggr)` and `aggr`) and the pure `resolveAlerts(vec, idx, clusterResolver) (map[string][]graph.Alert, unmatched, ambiguous int)` applying the kind precedence pod > pvc > node > aggr, the `{cluster, node}` two-way test, the identity ladder and the unique-in-estate fallback; verify `alerts_test.go` covers every scenario of the `alert-overlay` "Label-set matching to graph nodes" requirement and that output is sorted / de-duplicated on `(name, severity)`.
- [ ] 4.3 Attach the resolved alerts to the node structs in `Build` (and later `BuildStorage`) after `assemble` and before `graph.NewGraph`, and emit the aggregated `alerts_unmatched` / `alerts_ambiguous` Warns only when non-zero and the vector was non-empty; verify a `pkg/build` test asserts the Warn counts and that a `pending`-only vector attaches nothing.

## 5. Storage-flow build

- [ ] 5.1 Implement `pkg/build/storageflow.go` `assembleStorageFlow(topology) ([]graph.GraphNode, []*graph.Edge)`: materialise every inventory controller / aggregate / SVM plus the loaded pods, K8s nodes and PVCs; emit `storage-flow` edges per joined-and-mounted claim (`node-aggr`, `aggr-svm`, `svm-pvc`, `pvc-pod` × mounters, `pod-node` × scheduled mounters) deduplicated by `(source, target)` via insert-only sets, `labels.tier` set, `labels.attribution="split"` on `pvc-pod` edges of a claim with > 1 mounters, and the claim's full `IOMetrics` baked ONLY on `svm-pvc`; verify `storageflow_test.go` covers "One claim draws one path", "Shared upstream hops are emitted once", "FlexGroup claim starts at the SVM", an empty-`svm` claim contributing no path, an unscheduled pod ending at `pvc-pod`, and byte-identical edge ids across two runs.
- [ ] 5.2 Add `Builder.BuildStorage(ctx, window, end, sel)` reusing `ReadTopology`, skipping `ReadServiceGraph`, calling `assembleStorageFlow`, attaching alerts, setting `ClusterIdentities` and self-metrics; verify a `pkg/build` test with a mock querier asserts no `traces_service_graph_*` or `up` query is issued and that the returned graph contains no `service` / `external` node and no non-`storage-flow` edge.

## 6. Storage projection

- [ ] 6.1 Implement `graph.ProjectStorage(g, StorageScope) View`: flow-unit extraction from the `svm-pvc` edges, per-side root resolution (`ontap_cluster` label match, controller / aggregate / SVM / K8s-node names, `(namespace, name)` pods; `node=` on both sides), the per-side `requested → must hit` rule, re-applied `cluster` / `namespace` filters, retained node set ∪ root ids ∪ root-aggregate owners via `pullNetAppParents`; verify `project_storage_test.go` covers every scenario of "Root selectors from either end of the flow", "Roots are always materialised when the upstream knows them" (including `?aggr=typo` → empty) and "Storage-reachability projection".
- [ ] 6.2 Implement weight summation over retained units (share = claim flow ÷ `n` mounters counted in the built graph; ascending `(claim id, pod id)` order; `WithIO` copies; `svm-pvc` keeps latency / ceiling; unmeasured claims contribute nothing; all-unmeasured edges carry no `IO`); verify unit tests pin "Weights conserve through the chain", "RWX claim split across its mounters", "Latency and ceiling only on the claim-level edge" and "Unmeasured claim draws a weightless path".
- [ ] 6.3 Add a property test in `pkg/graph/property_test.go` generating random estates (claims with 0–3 mounters, random measured / unmeasured, random FlexGroup shapes) and random root sets, asserting at every non-root interior node that incoming and outgoing sums agree within 6-significant-digit rounding, retained ⊆ built, and byte-identical output across shuffled inputs; verify `go test ./pkg/graph/ -run Property` passes 1000 iterations.

## 7. Serialiser

- [ ] 7.1 Add `HardwareDTO`, `PerfDTO` (round6) and `AlertDTO` to `pkg/cytoscape` with `omitempty` fields `hardware`, `perf`, `alerts` on `NodeData`, and the `NodeTypeNetAppSVM → storage-cluster/<oc>` case in `compoundParent`; verify `cytoscape` unit tests pin the exact JSON of "Hardware and perf serialised", "Attribute shape" (alerts) and "SVM nests under the storage cluster", and that a node with none of the three serialises byte-identically to before.

## 8. Request parsing, engine, HTTP surface

- [ ] 8.1 Add `kubegraph.ParseStorageValues(url.Values) (StorageRequest, error)` sharing `parseTimestamp` / `validateSelectorValues`: `az` / `env` exactly once (`missing_az`, `missing_env`, `invalid_scope` on repeat), `pod=<ns>/<name>` split validation, single-element `Selector` slices, `edge_type` / `prune` ignored; verify `parse_storage_test.go` covers every 400 scenario of "Storage-flow graph endpoint" and "Malformed pod root".
- [ ] 8.2 Add `Engine.BuildStorage` and `Engine.BuildStorageFromValues` (parse → `BuildStorage` → `ProjectStorage` → `Serialise`); verify `engine_test.go` asserts the facade body equals the handler body for the same parameters and the same `reason` on failure ("Embedder and server agree").
- [ ] 8.3 Add `handleStorageGraph` in `internal/api` mirroring `handleGraph` (build-timeout wrapper, `mapBuildError`, project / serialise spans, structured request log) registered at `GET /v1/storage-graph` behind the API-key middleware, with swag annotations; verify `make docs` regenerates `docs/swagger.{json,yaml}` with the new operation and `make check-docs` is clean.
- [ ] 8.4 Add `internal/api` component tests via `newMockQuerier`: success body shape, `missing_az`, repeated `env`, 401 without key, `az` / `env` matchers and Harvest zone routing asserted on the recorded upstream queries, timeout → 504; verify `go test ./internal/api/ -run StorageGraph` passes.

## 9. Golden and integration coverage

- [ ] 9.1 Add goldens `storage-graph-aggr-root-cytoscape.json` (storage-side root) and `storage-graph-pod-root-cytoscape.json` (workload-side root, RWX split present) driven by a shared fixture with two clusters, a FlexGroup claim, an unmounted claim and an unmeasured claim; verify `go test ./internal/api/ -run Golden` passes without `-update` on a second run.
- [ ] 9.2 Extend the `with-netapp-storage-cytoscape.json` fixture with `node_labels`, the four counters and one `ALERTS` series per matched kind (pod, node, pvc, netapp-node, netapp-aggr) and regenerate that golden once; verify every OTHER existing golden is byte-unchanged (`git diff --stat internal/api/testdata/golden/` names only the one file).
- [ ] 9.3 Add `internal/integration` `TestStorageGraph`: ingest a two-SVM, two-aggregate estate with a shared RWX claim and firing `ALERTS`, request `/v1/storage-graph` from both root sides, and assert tier conservation, the split label, root-always for a claimless aggregate, empty body for an unknown root, and `data.alerts` / `data.hardware` / `data.perf` presence; extend `TestPVCNetAppHarvestJoin`'s fixture with `node_labels`; verify the suite passes with Docker available and skips cleanly without.
- [ ] 9.4 Add an integration case with a dedicated `alerts` backend in the routing table asserting the `ALERTS` query reaches only it and that a table without `alerts` still starts and serves both endpoints; verify via the recorded per-backend query log.

## 10. Documentation and containment

- [ ] 10.1 Update `docs/netapp-harvest-preconditions.md` (five node legs, `netapp-svm`, storage-flow tiers), `docs/upstream-metrics.md` (`ALERTS` contract, label preconditions, `{cluster, node}` ambiguity note), `docs/upstream-backend-routing.md` (`alerts` optional family), and `CLAUDE.md` (request surface, edge-type list, NetApp bullet, alert overlay, query-count pins); verify `make lint` and `make check-docs` pass and `grep -n 'five families\|all five' docs/ CLAUDE.md` returns only the required-family sentences.
- [ ] 10.2 Run `make test`, `make lint`, `make vuln`, `make check-route-containment` and `make check-parser-containment`; verify all green and that `pkg/build` still imports nothing from `internal/`.
