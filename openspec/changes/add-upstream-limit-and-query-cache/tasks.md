# Tasks

## 1. Query-result cache core (`pkg/promql`)

- [x] 1.1 Add an internal LRU (`pkg/promql/querycache.go`): `cacheKey{store uint64, query string, ts int64}`, `container/list` + map + mutex, cost `max(1, len(vec))`, per-entry TTL checked on read, oversized results not inserted, `maxSeries == 0` ⇒ nil cache. Verify: table-driven unit tests for hit, miss, TTL expiry (fake clock), LRU order, oversized result, zero budget — `go test ./pkg/promql -run TestQueryCache -race`.
- [x] 1.2 Return a fresh slice header (`slices.Clone`) on every hit; errors and cancelled contexts never inserted; empty vectors inserted. Verify: unit tests asserting appending to a returned vector does not change the next hit, and that an error result leaves no entry.
- [x] 1.3 Add `promql.CacheMetrics` optional upgrade interface (hits, misses, coalesced, evictions, resident series) with a no-op fallback, type-asserted like `RouterMetrics`. Verify: unit test with a recording fake counts each event once.

## 2. Per-store guard in the Router (`pkg/promql`)

- [x] 2.1 Add `guardedQuerier` wrapping one per-`clientKey` client: cache lookup → `singleflight.DoChan` (per-waiter `ctx` select, one-shot retry when the shared call returned a context error the waiter does not own) → `semaphore.Weighted` acquire (nil when limit 0) → inner `Instant`. Verify: unit tests with a blocking fake inner querier — N concurrent identical calls issue 1 upstream call; a waiter's own deadline returns without cancelling the leader; a leader cancellation makes a live waiter retry and succeed.
- [x] 2.2 Enforce the limit: in-flight never exceeds `n` per store, FIFO grant, cache hits and coalesced waiters hold no slot, a saturated store does not delay another store. Verify: unit test with a counting fake over 120 concurrent distinct queries and limit 32 (max observed ≤ 32), plus a two-store test.
- [x] 2.3 Wire guards into `routerState.byKey`: assign each `clientKey` a monotonically increasing store id; carry guard (semaphore + id) across `Swap` with the client; two backends naming one store share one guard. Add `RouterOption` variadic to `NewRouter` with `WithMaxConcurrency(n)` and `WithQueryCache(maxSeries, ttl)`; export `DefaultMaxConcurrency` (32), `DefaultQueryCacheMaxSeries` (100000), `DefaultQueryCacheTTL` (60s); no options ⇒ defaults ON; `0` disables; `ttl <= 0` with a positive budget ⇒ default TTL. Verify: `router_test.go` cases for swap-preserves-guard, shared-store bound, defaults-on with no options, and explicit disable; `go build ./...` shows every existing caller compiles unchanged.
- [x] 2.4 Export `promql.Guard(q Querier, opts ...RouterOption) Querier` wrapping one plain store with the same guard and defaults. Verify: unit test — `Guard(mock)` with no options coalesces identical calls and bounds concurrency at 32.
- [x] 2.5 Bypass guard for `FamilyProbe` in `fanoutQuerier.issue` and in `Router.ProbeAll`. Verify: unit test — with the store saturated by blocked queries, `ProbeAll` and a probe-family `Instant` return immediately and never populate the cache.
- [x] 2.6 Add `promql.LimiterMetrics` optional upgrade (`AddInflight(backend, delta)`, `ObserveSlotWait(backend, seconds)`), backend label from the routing-table name passed down by `fanoutQuerier`; stash slot wait in ctx and have `Client.Instant` set `kube_state_graph.slot_wait_ms` on its span when non-zero. Verify: recording-fake metrics test; `client_span_test.go` case asserting the attribute; existing span tests unchanged.
- [x] 2.7 Assert cache hits record no `ObserveQueryDuration` / `IncQueryFailure` / `ObserveQuerySeries`. Verify: unit test with the recording metrics fake.
- [x] 2.8 Update `docs/upstream-backend-routing.md` with the library defaults and opt-outs, the concurrency limit, store-identity sharing, probe bypass and sizing rule (`limit × replicas ≤ -search.maxConcurrentRequests`). Verify: doc section present and reviewed.
- [x] 2.9 Audit existing tests that build a `Router` via `NewRouter` (`pkg/promql`, `internal/integration`, `cmd/`) for fixture swaps between identical queries at the same instant; add `WithQueryCache(0, 0)` only where a test relies on a fresh read, never globally. Verify: `go test ./pkg/promql/... ./internal/integration/... -race` green.

## 3. Shared-sample safety (`pkg/build`)

- [x] 3.1 Audit every reader in `pkg/build` (and `pkg/promql` scope/labelquery helpers) for writes through a returned sample (`s.Metric[...] =`, `delete(s.Metric, …)`, `s.Value =`, in-place sort of a shared vector's samples); fix any to copy first. Verify: `grep -rnE 'Metric\[[^]]+\] *=|delete\([a-z]+\.Metric|\.Value *=' pkg/build pkg/promql` yields only reviewed, non-shared sites (listed in the PR description).
- [x] 3.2 Regression test: run a representative `/v1/graph` and `/v1/storage-graph` build twice through a Router with the cache enabled over a mock factory, snapshot fingerprint+value of every cached sample before and after, assert unchanged and bodies byte-identical to a cache-off run. Verify: `go test ./pkg/build -run TestCachedSamplesNotMutated -race`.

## 4. End-time alignment (`pkg/kubegraph`, `internal/api`)

- [x] 4.1 Add `kubegraph.AlignWindow(start, end, grid)` (Unix-epoch floor on `UnixNano`, shift `start` by the same delta, identity for `grid <= 0`). Verify: table-driven tests including the spec scenarios (`12:19:47Z`→`12:19:30Z` keeping `15m30s`; `start=12:19:40Z,end=12:19:50Z` → 10s window ending `12:19:30Z`; grid 0 identity; a 7s grid to prove epoch, not zero-time, alignment).
- [x] 4.2 Add `kubegraph.DefaultEndAlign` (30s) and `kubegraph.Options` fields `EndAlign`, `MaxConcurrency`, `QueryCacheMaxSeries`, `QueryCacheTTL` (zero ⇒ exported default, negative ⇒ disabled, positive ⇒ value); apply `EndAlign` in `BuildFromValues` / `BuildStorageFromValues` after parsing. Verify: facade tests asserting the rendered-query instant for zero (aligned to 30s), negative (verbatim) and positive values.
- [x] 4.3 In `kubegraph.New`, keep a `promql.QuerierSource` (Router) as-is and wrap any other `Querier` with `promql.Guard` from the three `Options` fields; `Engine.Probe` uses the unwrapped querier. Verify: facade tests — a mock passed to `New` with zero options sees one upstream call for two identical builds; with `QueryCacheMaxSeries: -1` sees two; a Router passed in still routes by `az` (`QuerierFor` reached); `Probe` never cached. Check `graph-api-gateway`-style usage in `pkg/kubegraph` examples/tests and update any that assert verbatim `end`.
- [x] 4.4 Apply `cfg.EndAlign` in `internal/api` graph and storage-graph handlers after `parseGraphRequest` / `parseStorageGraphRequest`, before the build. Verify: component tests (`newMockQuerier`) asserting the `ts` the mock receives for both endpoints; validation-error tests still report the caller's values.
- [x] 4.5 Make component and golden test helpers explicit about alignment (set `EndAlign` in `newServerWithMocks*` overrides where a test asserts timestamps) and confirm goldens are unchanged. Verify: `go test ./internal/api/ -run Golden` passes without `-update`.

## 5. Configuration and wiring (`internal/config`, `internal/observability`, `cmd/`)

- [x] 5.1 Add `UpstreamMaxConcurrency`, `QueryCacheMaxSeries`, `QueryCacheTTL`, `EndAlign`, with `config.Defaults()` reading `promql.Default*` / `kubegraph.DefaultEndAlign` (never duplicated literals), with flags and `KSG_*` env vars; validate non-negative and TTL > 0 when the cache is on. Verify: `internal/config` tests asserting defaults equal the exported constants, flag/env precedence and each rejection.
- [x] 5.2 Implement the new self-metrics in `internal/observability` (`kube_state_graph_upstream_inflight{backend}`, `kube_state_graph_upstream_slot_wait_seconds{backend}`, `kube_state_graph_query_cache_{hits,misses,coalesced,evictions}_total`, `kube_state_graph_query_cache_series`) and adapters satisfying `LimiterMetrics` / `CacheMetrics`. Verify: metrics test scrapes the new names; existing `kube_state_graph_upstream_query_*` label sets asserted unchanged.
- [x] 5.3 Pass `WithMaxConcurrency` / `WithQueryCache` from config in `cmd/kube-state-graph` `buildRouter`, and `EndAlign` to the API server. Verify: `make build` and a startup log line listing the effective limit / cache budget / align grid.
- [x] 5.4 Integration: with the testcontainers VictoriaMetrics suite, issue concurrent identical `/v1/graph` requests and assert one upstream query per leg (VM `vm_http_requests_total` delta or a counting proxy) and identical bodies; with limit 1, assert all requests still succeed. Verify: `go test ./internal/integration -run TestUpstreamLimitAndCache` (Docker).

## 6. Contracts and docs

- [x] 6.1 Update CLAUDE.md load-bearing rules: replace "No server-side result cache" / "no singleflight" with the query-result cache + coalescing rule, replace "No time-window alignment" with the `--end-align` rule, and update the request-lifecycle diagram. Verify: `grep -n "singleflight\|alignment" CLAUDE.md` reflects the new rules.
- [x] 6.2 Add a `docs/BREAKING.md` entry for default end alignment, caching and limiting — for operators (`--end-align=0`, `--query-cache-max-series=0`, `--upstream-max-concurrency=0`) and for embedders (`WithMaxConcurrency(0)`, `WithQueryCache(0, 0)`, `kubegraph.Options` fields `-1`). Also update the CLAUDE.md "Reusable `pkg/` graph engine" section with the new constructors and defaults. Verify: both entries present.
- [x] 6.3 Update swag annotations / `docs/` if handler descriptions mention verbatim `start`/`end`. Verify: `make check-docs` clean.
- [x] 6.4 Full gate. Verify: `make test vet lint verify-mocks check-route-containment check-parser-containment` all pass; `openspec validate add-upstream-limit-and-query-cache --strict` passes.
