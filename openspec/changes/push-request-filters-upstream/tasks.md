# Tasks — push-request-filters-upstream

## 1. Pre-flight

- [x] 1.1 Confirm `replace-storageclass-with-netapp-nodes` is archived (`openspec list` no longer shows it; `openspec/specs/netapp-storage-graph/spec.md` exists) and rebase this branch on that state; verify `make test` is green before any edit.
- [x] 1.2 Capture the pre-change baseline: run the existing `internal/api` goldens and a throwaway test that prints `promql.Render(q, w)` for every `Query` constant into `testdata/render-baseline.txt` (committed), so task 2.5 and task 10.2 can diff against it.

## 2. pkg/promql — selector value, dimension table, renderer

- [x] 2.1 Add `LabelKeys{AZ, Env string}` with `DefaultLabelKeys()` and `(LabelKeys).orDefault()`, and `Selector{AZ, Env, Cluster, Namespace []string}` with `Active() bool`; unit-test `Active` on every single-dimension case and the empty value.
- [x] 2.2 Add the static `queryDims` table (bitmask `dimAZ|dimEnv|dimCluster|dimNamespace`) covering every `Query` constant per design D1 (pod/claim/Service/EndpointSlice/kubelet = all four; `kube_node_*` = AZ|Env|Cluster; Harvest = AZ|Env; trace queries and `QUpProbe` = none); add `TestQueryDims_EveryQueryListed` that iterates all constants and fails on a missing entry, and a test asserting the three trace queries and `up` map to zero dims.
- [x] 2.3 Implement `(Selector).render(dims, keys)`: de-dup + sort, single value → `key="v"` with `\` / `"` escaped, multi → `key=~"…|…"` with `regexp.QuoteMeta`, `Cluster` value `unknown` → `""` before rendering, fixed dimension order `az, env, cluster, namespace`; table-test single / multi / `unknown`-alone (`cluster=""`) / `unknown`-mixed (`cluster=~"alpha|"`) / metacharacters / custom keys.
- [x] 2.4 Change `Render` to `Render(q Query, window time.Duration, keys LabelKeys, sel Selector) string`, composing `fixed + request matchers` inside one `{...}` (omitted when both empty) for every case; delete `QClusterDiscovery`, `ClusterDiscoveryLookback`, and their `Render` case; update `queries_test.go` so each query is asserted with an empty selector and with a representative non-empty one (fixed selector preserved ahead of request matchers — `kube_node_status_addresses{type=~"ExternalIP|InternalIP",az="zone-a",cluster="cluster-alpha"}`, `qos_read_ops{lun="",az="zone-a"}`).
- [x] 2.5 Add `TestRender_EmptySelectorMatchesBaseline` that renders every constant with `Selector{}` / default keys and diffs against `testdata/render-baseline.txt` (minus the removed `cluster_discovery` line); verify it passes.

## 3. pkg/graph — scope and projection

- [x] 3.1 Reduce `Scope` to `{Clusters, Namespaces, EdgeTypes, Inventory bool}`; `NewScope(clusters, namespaces, edgeTypes []string, inventory bool)`; delete `Names`, `Root`, `Depth`, `Direction`, `MaxTraversalDepth`, `Direction` type/constants, `NameFilterActive`; fix compile errors across `pkg/` and `internal/`.
- [x] 3.2 Delete `traverse` and the `reachable` plumbing in `Project` / `filterNodes` / `filterEdges`; compute `excluded` unless `scope.Inventory`; unit-test that `Scope{}` still prunes and `Scope{Inventory: true}` emits an edgeless pod with its `pod-to-node` / `pod-mounts-pvc` / `pvc-to-netapp-aggr` chain and an unmounted PVC.
- [x] 3.3 Rewrite `infraNodePassesFilters` / `netappInfraPassesFilters` per design D4 (cluster check → inventory lift → referenced); unit-test the lift matrix: K8s node admitted unreferenced with `Inventory` and no namespace filter, not with a namespace filter; NetApp aggr/node admitted unreferenced only with `Inventory` and neither cluster nor namespace filter; `pullNetAppParents` still pulls the controller for an inventory-admitted aggregate.
- [x] 3.4 Update `readdEdgePartners` / `filterEdges` comments (name-anchored case gone) and verify `TestProject_*` for cluster/namespace/edge_type filters still pass unchanged; delete `TestProperty_TraversalDepthRespected` and `TestProperty_NameFilterEveryNodeMatchesOrIsRehydratedPartner`; add `TestProperty_PrunedViewSubsetOfInventory` (every node/edge of `Scope{}` appears in `Scope{Inventory: true}` for the same random graph).

## 4. pkg/build — selector threading, filtered-build rules

- [x] 4.1 Add `Options.LabelKeys promql.LabelKeys`; change `Builder.Build(ctx, window, end, sel promql.Selector)`, `ReadTopology(ctx, q, window, end, keys, sel)`, `ReadServiceGraph(..., sel)`; every `fetch` / `fetchOptional` and the three trace reads call the new `Render`; `upProbe` and the `outside_retention` log lines pass `Selector{}`; verify `go build ./...` and existing build tests (updated to pass `Selector{}`) are green.
- [x] 4.2 Gate the outside-retention branch on `!sel.Active()` (design D7), add `selector_active` to the `graph built` log, and add the `selector_family_empty` Warn (selector active, `kube_pod_info` returned rows, a kubelet or Harvest family returned zero); unit-test: filtered build with zero pods/nodes returns an empty graph with no `up{}` call on the mock; unfiltered still returns `ReasonOutsideRetention`.
- [x] 4.3 `newSGResolver(topology, filtered bool)`; implement design D5 in `resolveClient` / `resolveServer` (UID not loaded ⇒ fall into the empty-UID branches when `filtered`); unit-test: filtered build never calls `synthPod` (assert `len(res.synthPods)==0` over a fixture with unloaded UIDs on both sides), unloaded UID with `"://"` label resolves to a loaded Service, with `server="unknown"` and an out-of-scope client is dropped, with a plain label becomes `external/<label>`, with an empty label drops the series; unfiltered fixture still synthesises.
- [x] 4.4 Implement the design D6 journal: `journal` entries for `external`, `materializeServiceNode`, `addServiceEdge`, `addRouteChainEdge`, `markIngressService` (with previous role), buffered `noteExternal`; `anyLoaded(id)` = `podByID[id] != nil || services[id] != nil`; in `parseWithResolver`, after `resolveServer`, `admitted` ⇒ commit, else rollback + `continue` before `pairs` / RED / link markers; the journal is inert when `!filtered`.
- [x] 4.5 Unit-test D6: (a) external↔external series leaves `externals`, `services`, `svcEdges`, `routeChainEdges`, `extReasons` byte-identical to their pre-series state; (b) an out-of-scope caller → loaded Service is admitted with the Service and its fan-out materialised and an `external/<client>` source; (c) a rejected series that hit `markIngressService` restores the previous role; (d) two orderings of the same vector produce identical `ServiceGraphResult`; (e) RED companions of a rejected series contribute nothing; (f) unfiltered parse output is byte-identical to the pre-change fixture results.
- [x] 4.6 Cross-cluster under a cluster filter: fixture with a loaded `cluster-alpha` pod calling an unloaded `cluster-beta` pod (`server="cart"`) yields `pod-calls-pod` to `external/cart` with `labels.cluster: "cluster-alpha"` and no `metrics`; `service-selects-pod` fan-out of a family service reaches loaded clusters only.
- [x] 4.7 Route-resolution interplay: `collectRouteQueriesWith` still skips out-of-scope clients (`lookupClientPod` nil) — pin with a prescan test; `routeprescan_test.go` / `routeresolve` tests updated for the new `ReadServiceGraph` signature.

## 5. pkg/kubegraph — request parser and facade

- [x] 5.1 Replace the four-value return with `Request{Start, End time.Time; Scope graph.Scope; Selector promql.Selector}`; parse `cluster` / `namespace` / `az` / `env` into the selector **and** `cluster` / `namespace` into the scope; parse `prune` (`true` / `false`, default `true` → `Scope.Inventory = !prune`); drop `depth` / `direction` / `root` / `name` handling (no error when present); unit-test each parameter, the defaults, and that `?name=&root=&depth=9` parses without error.
- [x] 5.2 Add value validation per design D3 (≤253 bytes, no control characters, empty values skipped; `prune` strict) returning `invalid_scope`; table-test accepted / rejected values including `prod%0A` and `maybe`.
- [x] 5.3 `Options.LabelKeys`; `Engine.Build(ctx, window, end, sel)`; `BuildFromValues` threads `req.Selector` into `Build` and `req.Scope` into `Project`; unit-test `BuildFromValues` against a `MockQuerier` capturing `Instant` calls and assert a `?namespace=shop&az=zone-a` request renders `kube_pod_info{az="zone-a",namespace="shop"}` and leaves `traces_service_graph_request_total{client!~"user|unknown",server!~"user"}` unchanged.

## 6. internal/config + cmd — label keys

- [x] 6.1 Add `AZLabel` / `EnvLabel` to `Config` (defaults `az` / `env`), `KSG_AZ_LABEL` / `KSG_ENV_LABEL` in `applyEnv`, `--az-label` / `--env-label` flags; `Validate()` enforces `^[a-zA-Z_][a-zA-Z0-9_]*$` and distinctness with errors naming the setting; unit-test defaults, env override, flag-over-env, invalid key, identical keys.
- [x] 6.2 Wire `promql.LabelKeys{AZ: cfg.AZLabel, Env: cfg.EnvLabel}` into `build.Options` in `cmd/kube-state-graph/main.go`; update the `--api-timeout` help text (drop `/v1/clusters`); verify `./bin/kube-state-graph --az-label=zone --env-label=tier --prom-url=http://x` starts its flag parse cleanly (`make build` + `--help` output lists both flags).

## 7. internal/api — handler, removed endpoint, goldens

- [x] 7.1 `graphRequest` carries `scope` + `sel`; `parseGraphRequest` uses `kubegraph.Request`; `runBuild` passes `sel`; delete `handleClusters`, `clustersBody`, `ClusterInfo`, the `/v1/clusters` route and swag block; update `@Param` annotations (add `az`, `env`, `prune`; remove `name`, `root`, `depth`, `direction`); verify `go vet ./internal/api/` and that `GET /v1/clusters` returns 404 in `server_happy_test.go`.
- [x] 7.2 Update component tests that referenced `/v1/clusters` (`recovery_test.go`, `handlers_clock_test.go`, `error_sanitisation_test.go`, `server_happy_test.go`) to use `/readyz` or `/v1/graph` for the same behaviours; verify the package passes.
- [x] 7.3 Add `TestGraph_SelectorQueriesCaptured`: a `MockQuerier` recording every `Instant` query string for `?cluster=cluster-alpha&namespace=shop&az=zone-a&env=prod`; assert the exact strings for one query of each dimension class (pod, node, Harvest, kubelet, trace, `up` absent) and that the unfiltered request reproduces the baseline strings.
- [x] 7.4 Goldens: delete `name-filter-cytoscape.json` and its test case; add `prune-false-cytoscape.json` (edgeless pod with its node/claim/aggregate/controller chain + a podless NotReady node, projected through `Scope{Inventory:true}`) and `filtered-external-cytoscape.json` (inbound + outbound `external/<label>` partners of a namespace-filtered caller). **Deviation from the plan:** the golden harness serialises hand-built `graph.View`s rather than driving HTTP, so a `namespace-pushdown` / `az-env` golden would snapshot an ordinary graph and prove nothing about the push-down; those two are covered instead by the captured-query tests (7.3) plus the real-VictoriaMetrics FilterSuite (8.2). Regenerate with `-update` and verify every pre-existing golden is byte-identical.
- [x] 7.5 Component tests for status codes: filtered request with empty fixtures → 200 empty body and no `up` call; `?env=prod%0A` and `?prune=maybe` → 400 `invalid_scope`; `?name=x&root=y` → 200.

## 8. Integration tests (Docker)

- [x] 8.1 Extend the fixture helpers to stamp `az` / `env` on every topology family and `cluster` on kubelet series (defaults `zone-a` / `prod`, overridable per test); verify existing suites still pass with the stamped labels (unfiltered requests are unaffected).
- [x] 8.2 Add tests per the `container-integration` delta: `az` / `env` hit narrows the estate and `clusters`; `az` / `env` miss → 200 empty, not `outside_retention`; namespace push-down loads only the namespace's pods/claims/services with nodes and aggregates by reference; cross-namespace call → `external/<server label>`, two out-of-namespace pods → nothing; cluster filter → `external` partner; `prune=false` surfaces an idle pod's storage chain and (no filter) a podless node; `cluster=unknown` alone and mixed (`cluster=~"alpha|"`) matches unlabelled series; Harvest without `env` under `?env=` degrades to no NetApp nodes.
- [x] 8.3 Remove the `/v1/clusters` integration test and any `name=` / `root=` usage in `graph_e2e_test.go`, `red_metrics_test.go`, `route_e2e_test.go`, `auth_upstream_test.go`; verify the full suite passes on CI.

## 9. Docs and repository rules

- [x] 9.1 `make docs` → regenerate `docs/swagger.{json,yaml}` (no `/v1/clusters`, new parameters); verify `make check-docs` is clean.
- [x] 9.2 Update `README.md` and `README.zh-tw.md`: parameter list, `az` / `env` / `prune` semantics, flags table (`--az-label`, `--env-label`), remove `/v1/clusters` and the discovery query row, add the label precondition note and the filtered-build external rule; verify no remaining `v1/clusters` / `?name=` / `root=` mention (`grep`).
- [x] 9.3 Update `CLAUDE.md`: rewrite the "No filters pushed to PromQL" rule as the selector/projection taxonomy, the request lifecycle diagram (`Build(ctx, window, end, sel)`), the default-projection bullet (`prune`, no `name` / `root`), the `/v1/clusters` mentions, the `graph.Scope` / `ParseValues` descriptions, and the open-paths list; verify by reading the affected sections end to end.
- [x] 9.4 Draft the release note (BREAKING: `/v1/clusters` removed; `name` / `root` / `depth` / `direction` withdrawn and ignored; new `az` / `env` / `prune`; label precondition; filtered-build `external` rule; `pkg/kubegraph` signature changes for in-process embedders) in the PR description.

## 10. Verification

- [x] 10.1 `make test`, `make vet`, `make lint`, `make verify-mocks`, `make check-route-containment`, `make check-docs` all green locally.
- [x] 10.2 Diff proof of byte-identical unfiltered behaviour: `TestRender_EmptySelectorMatchesBaseline` green and every pre-existing golden unchanged (`git diff --stat internal/api/testdata/golden/` shows only the four new files and one deletion).
- [x] 10.3 Manual smoke against a dev VictoriaMetrics: `?namespace=<ns>&prune=false` returns the namespace's pods, nodes, claims, aggregates and controllers; `?env=<env>` narrows `clusters`; `?cluster=<c>` renders other-cluster peers as `external`; `/v1/clusters` is 404; record the three rendered query strings from the debug log in the PR.
