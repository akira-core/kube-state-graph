## 1. Compatibility baseline (record first — it must hold before AND after)

- [x] 1.1 On the unmodified tree run `go test ./internal/api/ -run TestGolden`, `go test ./pkg/...` and `go test ./internal/integration/ -run 'TestGraphSuite|TestMultiBackendSuite'` (Docker); verify all green. No golden or `pkg/` fixture carries the zone/environment pair and `MultiBackendSuite`'s `mb-alpha` carries `az` only, so these same runs — with NO `-update` and NO fixture edit — are the proof in 6.1 / 8.1 that an unstamped estate is byte-identical (design D8). Only `internal/integration/filtered_e2e_test.go` is expected to move (task 6.2).

## 2. `pkg/graph` — identity table and raw-name projection

- [x] 2.1 Add `type ClusterIdentity struct{ AZ, Env, Name string }`, the exported nil-safe field `Graph.ClusterIdentities map[string]ClusterIdentity` (identity → components) and `func (g *Graph) ClusterRawName(id string) string` (the entry's `Name`, else `id` verbatim) to `pkg/graph/graph.go` (design D7); leave `NewGraph`'s signature untouched; verify `go build ./...`, that `pkg/graph` gained no import, and `TestGraph_ClusterRawName` in a new `pkg/graph/identity_test.go` covers nil table / present / absent / empty id.
- [x] 2.2 Change the two `scope.Clusters[labels["cluster"]]` checks in `pkg/graph/project.go` (`nodePassesFilters`, `infraNodePassesFilters`) to compare `g.ClusterRawName(labels["cluster"])`, threading `g` (or a raw-name lookup closure built once in `Project`) into both predicates (design D3); verify every existing `pkg/graph/project*_test.go` test passes unchanged (a nil table degrades to today's comparison).
- [x] 2.3 Add `TestProject_ClusterFilterMatchesRawComponent` to `pkg/graph/project_test.go`: a graph with pods `us-dev-c1/p1`, `eu-prod-c1/p2`, `us-dev-c2/p3` and a table mapping each identity to its components; `NewScope(clusters={"c1"})` admits p1 and p2 and drops p3; `clusters={"us-dev-c1"}` (an identity, not a raw name) admits nothing; a K8s node `us-dev-c1/w0` referenced by p1 passes `infraNodePassesFilters` under `{"c1"}`; verify all three assertions pass.

## 3. `pkg/build` — the cluster resolver and the two-pass identity table

- [x] 3.1 Replace `missingClusterCounts` with a `clusterResolver` struct in `pkg/build/topology.go` holding `keys promql.LabelKeys`, `identities map[string]graph.ClusterIdentity`, `byRaw map[string][]string` (sorted, deduplicated), the existing missing-cluster tally and a new unresolved tally; give it `bucket(metric promql.Query, m model.Metric) string` implementing the ladder — compose when `m[keys.AZ]` and `m[keys.Env]` are both non-empty, adopt when `len(byRaw[raw]) == 1`, else verbatim + tally — and `warn()` emitting the existing missing-cluster line plus one aggregated `cluster_identity_unresolved` per metric (design D1, D2); verify `go build ./pkg/build/` fails only at the not-yet-migrated call sites.
- [x] 3.2 Add `func (r *clusterResolver) observe(m model.Metric)` (first pass: record a step-1 composition into `identities` and `byRaw`, no-op otherwise) and call it in `parseTopology` over `v.Pod`, `v.Node`, `v.Service` and `v.PVC` BEFORE any `bucket` call; change `parseTopology(v topologyVectors)` to `parseTopology(v topologyVectors, keys promql.LabelKeys)` with `keys.OrDefault()` on entry, pass `keys` from `ReadTopology`, and update the eight test call sites (`determinism_test.go`, `missing_cluster_warn_test.go`, `netapp_test.go`, `topology_fixes_test.go`) to pass `promql.LabelKeys{}`; verify `go vet ./pkg/build/`.
- [x] 3.3 Migrate every call site from `mc.bucket(q, string(s.Metric["cluster"]))` to `mc.bucket(q, s.Metric)` — 17 in `topology.go`, 2 in `resolvePVCUsage` (`netapp.go`), 1 in `parseServiceGraph` (`servicegraph.go`) — and carry the resolver on `Topology` (unexported field or `Topology.Clusters` accessor) so `ReadServiceGraph` / `parseServiceGraph` use the topology's instance instead of a fresh `missingClusterCounts{}`; verify `grep -c 'mc.bucket(' pkg/build/topology.go pkg/build/netapp.go pkg/build/servicegraph.go` still reports 17 / 2 / 1 and `grep -rn 'string(s.Metric\["cluster"\])' pkg/build` reports only the six ONTAP-cluster reads in `netapp.go` (`oc` — a filer, never a Kubernetes cluster) plus the prescan's trace-cluster read, which task 4.2 migrates.
- [x] 3.4 In `Builder.Build`, set `g.ClusterIdentities = topology.ClusterIdentities` immediately after `graph.NewGraph`; verify `TestBuild_ClusterIdentitiesReachTheGraph` in `pkg/build/build_test.go` (`newMockQuerier` fixtures: `c1` under `az="us",env="dev"` and under `az="eu",env="prod"`, `cluster-beta` unstamped) asserts `g.ClusterNames() == ["cluster-beta","eu-prod-c1","us-dev-c1"]`, `g.ClusterRawName("us-dev-c1") == "c1"`, and `g.ClusterRawName("cluster-beta") == "cluster-beta"`.
- [x] 3.5 Add `TestParseTopology_ClusterIdentity` subtests in a new `pkg/build/identity_test.go` pinning every `cluster-topology-source` scenario: same raw name in two zones → two node ids; every structure (pod id, PVC id, `ServicesByNameNS` key, `labels.node`, `ClustersObserved`) carries the identity; unambiguous kubelet series adopts and joins (`resolvePVCUsage`), no Warn; ambiguous `kube_pod_owner` stays `c1`, joins nothing, exactly one `cluster_identity_unresolved` Warn naming the metric (reuse the slog capture of `missing_cluster_warn_test.go`); az-only series never composes (`us--c1` / `us-c1` absent); the table is built from the four entity families only (a `kube_pod_owner` with a different pair composes by step 1 and creates no cluster); cluster-less series with the pair → `us-dev-unknown`; rebound `LabelKeys{Env: "deployment_tier"}` reads the rebound key; unstamped fixture → empty table, no Warn; verify every subtest passes and the two series orders of the "ambiguous" case produce identical output.
- [x] 3.6 Add `TestParseTopology_UnstampedIsByteIdentical` to `pkg/build/determinism_test.go`: feed the existing unstamped fixture through the new parse and `reflect.DeepEqual` the `Topology` (ids, labels, indexes) against a snapshot taken with a stub resolver that never composes; verify it passes — this is the in-package half of design D8.

## 4. Service graph and route boundary

- [x] 4.1 In `parseServiceGraph`, take `traceCluster` from the topology resolver's `bucket(promql.QServiceGraphTotal, s.Metric)` (composes when the collector stamped the pair, adopts otherwise) and set the edge's `srcCluster` to `clientPod.Labels()["cluster"]` when the client resolved to a topology pod, else to `traceCluster` (design D5); make `betterSrcCluster` compare the raw component (`us-dev-unknown` must still lose to `us-dev-c1`) and note in its comment that the tie-break is now reachable only for synthesised clients, since every duplicate series of one topology pod yields the same identity; verify `TestParseServiceGraph_EdgeClusterIsClientPodIdentity` in `pkg/build/servicegraph_test.go` covers the `pod-service-graph` scenarios "Edge label carries the client pod's identity" and "Synthesised client resolves the trace label through the ladder" (both the unambiguous and the ambiguous branch), and that every existing `servicegraph*_test.go` test passes unchanged.
- [x] 4.2 Resolve every cluster name crossing the route boundary through the same resolver: `RouteRequest.CallerCluster` is the client pod's identity (already so once 3.3 lands — assert it), and in `routeIndexResolve` / `resolveRouteChain` pass `dest.Cluster` and the ingress identity's cluster through `bucket`-equivalent resolution (compose is impossible — no labels — so adopt-or-verbatim) BEFORE the `ServicesByNameNS` lookup (design D9); verify `TestRoutePrescan_DestClusterResolvesThroughIdentity` in `pkg/build/routeprescan_test.go`: a store answering raw `c1` resolves to `us-dev-c1` when unique and degrades with the existing `route_engine_dest_cluster_lacks_service` reason when ambiguous; no new reason string is introduced.
- [x] 4.3 Pin the family rule over identities in `pkg/build/clusterfamily_test.go`: `ClusterFamilyKey("us-dev-c1") == ClusterFamilyKey("us-dev-c2")`, `!= ClusterFamilyKey("eu-prod-c1")`, `!= ClusterFamilyKey("us-prod-c1")`, and the documented caveat `ClusterFamilyKey("us-east-1-prod-c1") == ClusterFamilyKey("us-east-2-prod-c1")` (design D4) so a future struct-aware key is a deliberate test edit; verify the four assertions pass with NO change to `ClusterFamilyKey`.
- [x] 4.4 Add `TestParseServiceGraph_FanOutStaysWithinZoneFamily` to `pkg/build/servicegraph_test.go`: `us-dev-c1` and `us-dev-c2` both hold `payments-ns/payments`, so do `eu-prod-c1` / `eu-prod-c2`; a `"://"` call from a `us-dev-c1` pod fans `service-selects-pod` out to the `us-dev-*` backing pods only; verify no `eu-prod-*` target appears.

## 5. `pkg/cytoscape` and self-metrics — assert, do not change

- [x] 5.1 Add `TestSerialiseCytoscape_ClusterIdentityGroups` to `pkg/cytoscape/compound_test.go`: nodes of `us-dev-c1` and `eu-prod-c1` produce two cluster groups `cluster/us-dev-c1` and `cluster/eu-prod-c1` with `name` equal to the identity, namespace groups `us-dev-c1/namespace/<ns>`, and `clusters == ["eu-prod-c1","us-dev-c1"]`; verify it passes with zero edits to `cytoscape.go` (design D1 — the serialiser inherits the identity).
- [x] 5.2 Extend `pkg/graph/graph_counts_test.go` with a case where `us-dev-c1 → eu-prod-c1` counts as a cross-cluster `pod-calls-pod` bucket and `NodeCountByKind` keys by identity; verify it passes unchanged against `EdgeCountByType` / `NodeCountByKind` (design D10).

## 6. Golden and integration

- [x] 6.1 Add a `cluster-identity` scenario to `TestGolden_GraphResponses` in `internal/api/golden_test.go` (a `buildClusterIdentity()` view with `us-dev-c1` and `eu-prod-c1` each holding a pod / node / PVC / service, `cluster-beta` unstamped, one intra-family and one cross-identity `pod-calls-pod` edge, and `g.ClusterIdentities` populated for the two identities); run `go test ./internal/api/ -run TestGolden -update` ONCE, then verify `git status --porcelain internal/api/testdata/golden/` shows exactly one added file (`cluster-identity-cytoscape.json`) and zero modified files.
- [x] 6.2 Update `internal/integration/filtered_e2e_test.go`: its `ExtraLabels` stamps `az="zone-a",env="prod"` on every series and `cluster-beta` carries `az="zone-b"`, so move every id / `labels.cluster` / `clusters[]` assertion from `cluster-alpha/…` to `zone-a-prod-cluster-alpha/…` (and `zone-b-prod-cluster-beta/…`), keeping every `?cluster=cluster-alpha` REQUEST value raw; verify `go test ./internal/integration/ -run TestGraphSuite` is green under Docker and that `MultiBackendSuite` passes with NO edit (its `mb-alpha` never composes).
- [x] 6.3 Add `TestClusterIdentitySplitsSameRawName` to a new `internal/integration/identity_e2e_test.go` on the VM suite: ingest `c1` under `az="us",env="dev"` and `az="eu",env="prod"` (pods, nodes, a claim each, a service-graph call between them, and one `kubelet_volume_stats_used_bytes{cluster="c1"}` series WITHOUT the pair); assert on `GET /v1/graph?prune=false`: two cluster groups, `clusters == ["eu-prod-c1","us-dev-c1"]`, no element carries `labels.cluster: "c1"`, the cross-identity edge is present with `labels.cluster: "us-dev-c1"`, the unstamped kubelet series joined neither claim, and the server log holds one `cluster_identity_unresolved` line naming `kubelet_volume_stats_used_bytes`; then `?cluster=c1` returns both identities, `?az=us&env=dev&cluster=c1` returns `us-dev-c1` only with the kubelet usage now joined (the raw name is unique in that build), and `?cluster=us-dev-c1` returns an empty 200 (spec "A listed identity is not a cluster filter value"); verify green under Docker.

## 7. Documentation

- [x] 7.1 Add a `## Cluster identity is \`<az>-<env>-<cluster>\`` section to `docs/BREAKING.md` covering: id prefixes and `labels.cluster` on stamped estates; `clusters[]` lists identities that are NOT `?cluster=` values (pin with the triple); edge `labels.cluster` is the client pod's identity; same raw name across zones is now cross-cluster; the route store should write identity names; the unstamped estate is unchanged; verify the section names no Go identifier.
- [x] 7.2 Update `CLAUDE.md`: rewrite the "Cluster-scoped IDs everywhere" bullet around the identity and the ladder (compose / adopt / verbatim + `cluster_identity_unresolved`), amend the D9 sentence on edge `labels.cluster`, note the family key runs over the identity string with the D4 caveat, and state that `?cluster=` stays raw at both layers via `Graph.ClusterRawName`; verify the bullet cross-references `clusterResolver`, `Graph.ClusterIdentities` and `ClusterRawName` by their final names.
- [x] 7.3 Extend `docs/kube-state-metrics-preconditions.md` §"What KSM cannot supply — `cluster`, `az`, `env`": the pair now defines identity, every cluster-keyed family must carry both or it resolves through the ladder (unique → joins; ambiguous → orphan `c1` cluster + Warn), plus the sanity query `count by (cluster, az, env) (kube_pod_info)` expecting one row per cluster; verify the query runs against the demo's vmauth store.
- [x] 7.4 Add the route-store naming contract (write the identity string in the cluster column; a raw name resolves only while unique in the build) to the route documentation that describes the store's cluster column; verify no OpenAPI drift after `make docs` via `git diff --quiet -- docs/swagger.json docs/swagger.yaml` (the wire types are unchanged); `make check-docs` diffs the WHOLE `docs/` tree, so it only passes once these prose edits are committed.

## 8. Final verification

- [x] 8.1 Run `make lint`, `make vet`, `make test` (race + shuffle), `make verify-mocks`, `make check-route-containment` and `make check-parser-containment`; verify all green and that `git status` shows exactly one new golden (6.1) and only the intended integration edits (6.2, 6.3).
- [x] 8.2 From the demo repository run `make redeploy-backend BACKEND_SRC=./kube-state-graph` (the SUBMODULE path — `../kube-state-graph` is a different sibling working copy and builds a binary without `--backends-file`, which CrashLoops) then `make verify`; verify green, and confirm in Grafana (<http://localhost:3001>) that the cluster compound and every `cluster` label read `<az>-<env>-<cluster>` from `global.ksgExternalLabels`, that `clusters[]` in `make graph` lists the identity, and that the dashboard's `cluster` dropdown (sourced from `kube_pod_info`, the raw name) still narrows the graph.
