## 1. Parity pins (write first — they define "candidate generator, not judge")

- [x] 1.1 Extend `TestBuildStorage_PlanIsOutputPreserving` in `pkg/build/build_storage_plan_test.go` with an Application-rooted case: a Deployment-managed claimless pod (`kube_deployment_annotations` → `kube_replicaset_owner` → `kube_pod_owner`), a CronJob-managed claimless pod (`kube_cronjob_annotations` → `kube_job_owner` → `kube_pod_owner`), a StatefulSet-managed mounting pod resolving the same Application, and a StatefulSet carrying two tracking-ids (`alpha:…`, `beta:…`) whose pod must resolve `alpha`. Build through `storagePlan(roots{Applications:{beta}})` and through a hand-fed `fullPlan` graph projected with the same scope; serialise; assert byte equality and that the `beta` root retains nothing from the two-tracking-id StatefulSet (design D1, spec "An over-admitted pod is loaded and dropped"). Verify the test is RED today (the claimless pods are not loaded) and stays a compiling fixture for group 5.
- [x] 1.2 Extend the scoped-query capture helper from `pkg/build/controllerscope_test.go` so it also records, per query, the values of an `annotation_argocd_argoproj_io_tracking_id=~` matcher (parsing the `(?:a|b)(?::.*)?` group back to `[]string{"a","b"}`) and the `owner_kind` / `owner_is_controller` / `owner_name` matchers. Verify a unit test round-trips one rendered string of each shape.

## 2. `pkg/graph`: the root set and the projection

- [x] 2.1 Add `Applications map[string]struct{}` to `StorageRoots` in `pkg/graph/storagescope.go` (doc: workload side, matched against pod AND claim `Application()`; pods materialise, claims retain only), include it in `RequestedWorkload()`, and add the `applications []string` parameter to `NewStorageScope` filled through `stringSet` (design D8). Verify `TestNewStorageScope_DeduplicatesAndDropsEmpties` covers `application` (`{"b","a","","a"}` → `{a,b}`; bare empty → nil) and `TestStorageRoots_RequestedPerSide` reports workload-requested for an applications-only root set.
- [x] 2.2 In `pkg/graph/project_storage.go` make `resolveStorageRoots` return a fourth set `claimHits` (PVCs whose `Application()` is a root value) and add pods whose `Application()` is a root value to `workload`; set `workloadExclusive = len(Pods) > 0 || len(Applications) > 0`; keep a unit iff `!workloadExclusive || u.intersects(workload) || u.intersects(claimHits)`; leave `admitRoot` over `workload` only; update the `default:` comment (design D7). Verify `go vet ./pkg/graph/` is clean and every existing `TestProjectStorage_*` passes unchanged.
- [x] 2.3 Add to `pkg/graph/project_storage_test.go`, using `stChain` / `stPod` / `stPVC` with `ApplicationValue` set: `TestProjectStorage_ApplicationRootFindsItsStorageAcrossNamespaces` (spec scenario of that name: two `checkout` pods in different namespaces on `aggr1` / `aggr2` retained, a `ledger` pod on `aggr1` absent), `TestProjectStorage_ApplicationRootMatchesClaimOwnApplication` (claim `billing` mounted by a `ledger` pod → whole path retained, the pod present only via the path — assert it is absent when the claim's `svm-pvc` edge is removed), `TestProjectStorage_ApplicationRootIntersectsStorageRoot` (`aggr1` + `checkout` → only the `aggr1` path), `TestProjectStorage_ApplicationAndPodRootsUnion`, `TestProjectStorage_StatelessApplicationStillShows` (three claimless `checkout` pods → three nodes, no edge), `TestProjectStorage_UnknownApplicationIsNotDrawn` (empty view), `TestProjectStorage_NamespaceFilterNarrowsApplicationRoot`, `TestProjectStorage_ApplicationClaimIsNeverMaterialised` (an unmounted PVC carrying the root Application is absent). Verify all pass and that 2.2's `claimHits` branch fails `…MatchesClaimOwnApplication` when removed (mutation-checked).

## 3. `pkg/kubegraph`: the parser

- [x] 3.1 In `ParseStorageValues` (`pkg/kubegraph/parse.go`) add `"application"` to the validated parameter list and pass `v["application"]` to `NewStorageScope`; make `deriveStorageNamespaces` return `explicit` when `len(scope.Roots.Applications) > 0` (design D8). Verify `go build ./...` succeeds (every `NewStorageScope` caller updated — `pkg/kubegraph`, `internal/api/golden_test.go`, tests).
- [x] 3.2 Add to `pkg/kubegraph/parse_storage_test.go`: happy path with `application=checkout&application=a` → `Roots.Applications == {a, checkout}`; the 300-byte and control-character rejections as `invalid_scope` (spec "Application root value is validated like every selector value"); a bare `application=` is a no-op; `TestParseStorageValues_ApplicationRootDoesNotDeriveNamespace` (`pod=shop/orders-0&application=checkout` → `Selector.Namespace` empty; spec "An application root does not derive a namespace"); explicit `namespace` still wins. Verify all pass and `TestStorageGraph_EmbedderAndServerAgree` in `internal/api` still passes.

## 4. `pkg/promql`: the two recovery renderers

- [x] 4.1 Create `pkg/promql/appscope.go` with `RenderTrackingIDScoped(q, window, keys, sel, apps) (string, bool)` (valid for the six annotation families; renders `{<fixed>,<request>,annotation_argocd_argoproj_io_tracking_id=~"(?:<a>|<b>)(?::.*)?"}` with `escapeLiteral(regexp.QuoteMeta(v))` per sorted, de-duplicated value; `ok=false` for any other query or an empty set) and `TrackingIDWrapperCost` (the rendered length of the wrapper a chunk is charged, for the caller's budget) (design D3). Verify `TestRenderTrackingIDScoped` pins: one value, two values, a value with a regex metacharacter and a quote, the fixed `!=""` present ahead of the request matchers, `ok=false` on `kube_pod_info`, and a property test that the output equals `Render(q,…)` with exactly one matcher appended.
- [x] 4.2 Add `RenderOwnerScoped(q, window, keys, sel, kind, names) (string, bool)` to the same file: `kube_replicaset_owner` → `owner_kind="<kind>",owner_name=~"…"`; `kube_pod_owner` → `owner_is_controller="true",owner_kind="<kind>",owner_name=~"…"`; `kube_job_owner` → `owner_name=~"…"` only, `ok=false` unless `kind == "CronJob"`; `ok=false` for any other query, an empty kind or an empty set (design D3). Verify `TestRenderOwnerScoped` pins each of the three shapes with and without an az/env/cluster/namespace selector, the `kube_job_owner` fixed selector preserved, and the "is `Render` plus N matchers" property.
- [x] 4.3 Add `TestRender_EmptySelectorMatchesBaseline` still passes with `pkg/promql/testdata/render-baseline.txt` byte-identical, and `TestQueryFamily_EveryQueryListed` / `TestQueryDims_EveryQueryListed` unchanged (no new `Query` constant). Verify with `git diff --exit-code pkg/promql/testdata/`.

## 5. `pkg/build`: the recovery wave

- [x] 5.1 In `pkg/build/topologyplan.go` add `applicationRoots []string` to `topologyPlan`, filled by `storagePlan` as `sortedNames(slices.Collect(maps.Keys(roots.Applications)))`; leave `restrictsVolumeLabels` untouched (design D9). Verify `go build ./...` and `TestReadTopology_FanOutLegCount` (37 / 43 for `fullPlan`) unchanged.
- [x] 5.2 Generalise `scopedFamily` in `pkg/build/scopedread.go` with an optional `render func(chunk []string) (string, bool)` hook (nil ⇒ `promql.RenderScoped` as today) and an optional `budgetOverhead int` passed to `ChunkScopeWithOverhead`, so `issueScopedFamilies` can issue the recovery families through the D3 renderers; keep every existing call site unchanged. Verify every `TestIssueScoped*` / `TestReadScoped*` test passes unchanged.
- [x] 5.3 Add `ExtraSeriesCount map[promql.Query]int` to `topologyVectors` with `addExtraSeries(v, scopeMu, q, n)` and make `tallySeries` add it into the family's entry, creating the entry when no other wave issued the family (design D4). Verify a unit test: by-reference 3 + extra 2 ⇒ 5; extra only ⇒ present with 2; neither ⇒ absent.
- [x] 5.4 Create `pkg/build/appscope.go` with the pure scope functions and `readScopedApplications(ctx, callerCtx, q, window, end, opts, sel, roots, v, scopeMu) ([]string, error)` (design D2): stage 1 issues the six annotation families with `RenderTrackingIDScoped`, modes Deployment / StatefulSet / DaemonSet / CronJob `legRequired`, ReplicaSet / Job `legOptional` with NO degraded flag (design D5), into wave-local vectors, then extracts per-kind name sets from the identity labels; stage 2 issues `kube_replicaset_owner` (`RenderOwnerScoped … "Deployment"`) and `kube_job_owner` (`… "CronJob"`), `legRequired`, empty set ⇒ not issued; stage 3 issues `kube_pod_owner` once per kind with a non-empty union (`ReplicaSet`, `Job`, `StatefulSet`, `DaemonSet`, `Deployment`, `CronJob`), `legRequired`; every stage records its merged series count through `addExtraSeries`; returns `sortedNames(pod labels)`. Verify `go vet ./pkg/build/` and `go test -race ./pkg/build/` are clean.
- [x] 5.5 Implement the stage-1 bound in the same file: `maxApplicationRootChunks = scopeConcurrency`; when `promql.ChunkScopeWithOverhead(roots, budget, …)` for a family exceeds it, issue that family once as `promql.Render(q, …)`, keep rows with `argoAppName(tracking-id) ∈ roots`, and log `application_root_restriction_unbounded` once per build with the family and the chunk count (design D6). Verify `TestReadScopedApplications_UnboundedRootSetReadsUnrestricted` (spec scenario): thousands of values ⇒ one unrestricted query per family, rows filtered, names identical to the restricted read's, log line emitted.
- [x] 5.6 Add the pure `podScopeUnderApp(bindings, pvcAnnotations model.Vector, recovered, podRoots, apps []string) []string` to `pkg/build/appscope.go` (design D10): keep set K = recovered ∪ podRoots; related claims C = claims with any `kube_persistentvolumeclaim_annotations` row whose `argoAppName` ∈ apps, ∪ claims of binding rows whose `pod ∈ K` (keyed `(cluster, namespace, claim)` through the binding reader's own `persistentvolumeclaim` / `claim_name` rule); scope = K ∪ pods of every binding row whose claim ∈ C; sorted, de-duplicated. Verify `TestPodScopeUnderApp` covers: unrelated binding pod excluded; own-annotated claim's mounters included; every co-mounter of a recovered pod's claim included; a `pod=` root's co-mounters included; a same-named claim in another namespace not treated as related; determinism across input order.
- [x] 5.7 Wire the wave in `readTopology` (`pkg/build/topology.go`): add a `pvcAnnotationsDone` signal for `promql.QPVCAnnotations` to the `signals` map; when `len(plan.applicationRoots) > 0` launch `readScopedApplications` under `signalWhenDone(…, appDone)` writing its names into a slot the pod wave reads after `appDone`, otherwise close `appDone` immediately; make `readScopedPods` take `appDone`, `pvcAnnotationsDone` and the slot, wait on `bindingsDone` AND `appDone` (AND `pvcAnnotationsDone` only under an application root), and compute the scope through `podScopeUnderApp` under an application root and `podScope` otherwise; `podsDone` unchanged (design D2, D10). Verify `TestReadScopedPods_*` pass unchanged with closed signals and no application root, a new `TestReadScopedPods_WaitsOnRecoveryAndPVCAnnotations` shows the pod query is not issued until both are written, and `TestReadScopedPods_UnrelatedBindingPodsNotRead` (spec scenario "Only related binding pods are read under an application root") asserts the exact `pod` alternation `{orders-0}`.
- [x] 5.8 Add `pkg/build/appscope_test.go` with `MockQuerier` fixtures: `TestReadScopedApplications_DeploymentManagedClaimlessPod` (spec scenario "A Deployment-managed stateless pod is recovered and drawn" — assert the stage-1 matcher values, the `kube_replicaset_owner` `owner_kind="Deployment"` + `owner_name=~"web"`, the `kube_pod_owner` `owner_is_controller="true",owner_kind="ReplicaSet",owner_name=~"web-7d9f"`, no `kube_job_owner`, and the returned names `{web-7d9f-abc}`), `TestReadScopedApplications_CronJobManagedPod` (spec scenario), `TestReadScopedApplications_NoControllerMatches` (six stage-1 queries, no stage 2 / 3, empty names), `TestReadScopedApplications_NamespaceFilterNarrows` (every query carries `namespace="shop"`), `TestReadScopedApplications_DegradingFamilyDegradesAlone` (stage-1 `kube_job_annotations` chunk error ⇒ nil error, `JobAnnotationsDegraded` NOT set, other names intact), `TestReadScopedApplications_RequiredStageFails` (stage-3 `kube_pod_owner` error ⇒ error), `TestReadScopedApplications_StagesAreOrdered` (stage 2 issued only after stage 1 returned; stage 3 after stage 2), `TestReadScopedApplications_ValueOrderIsIrrelevant` (`{b,a}` and `{a,b,a}` render identical queries). Verify all pass.
- [x] 5.9 Rewrite `TestBuildStorage_FanOutLegCount` in `pkg/build/build_storage_plan_test.go` to the design D11 table: rows without `application=` keep 18 / 22 / 25 / 27 / 32 / 38; `application=x` with nothing matching adds 6 and — with unrelated bindings present — issues NO pod query; one Deployment-managed claimless pod adds 6 + 1 + 1 then pods 2, nodes 4, controllers 3; one CronJob-managed claimless pod likewise; every kind present adds 6 + 2 + 6. Verify each count, and that removing the stage-3 `Deployment` / `CronJob` direct arms fails the every-kind row.
- [x] 5.10 Run parity pin 1.1 (extend its fixture with an unrelated mounting pod, an own-annotated claim mounted by a non-root pod, and an RWX claim co-mounted by a recovered pod and an `alpha` pod so the split weight is exercised — spec scenarios "Every mounter of a related claim is read" and "A co-mounter that sorts after the root is drawn through inheritance") and add `TestBuildStorage_OverAdmittedPodIsDropped` (spec scenario "An over-admitted pod is loaded and dropped": the two-tracking-id StatefulSet's pod is loaded — `kube_pod_info` restricted to its name — and absent from the projected body) and `TestBuildStorage_RecoveryTalliedUnderFamilyName` (spec scenario: stage-1 2 + by-reference 3 ⇒ `RawSeriesCount["kube_deployment_annotations"] == 5`). Verify all green and byte-identical to the whole-binding read.

## 6. `internal/api`: HTTP surface, OpenAPI, goldens

- [x] 6.1 Add the `application` `@Param` (query, `[]string`, `collectionFormat(multi)`, description "Workload root: ArgoCD Application name, as `data.application` carries it (the tracking-id segment before the first `:`); matches a path whose pod or claim carries it; every pod resolving it is drawn even with no claim") and extend the `**Roots**` `@Description` line of `handleStorageGraph` in `internal/api/handlers.go`; run `make docs`. Verify `make check-docs` passes and `docs/swagger.json` lists the parameter on `/v1/storage-graph`.
- [x] 6.2 Add a `storage-graph-application-root` golden to `internal/api/golden_test.go` from `buildStorageGraphEstate()` extended with one claimless pod resolving the root Application (materialised, no edge) and one mounting pod resolving it; project with `NewStorageScope(…, applications: {…})`; run `go test ./internal/api/ -update -run Golden` ONCE and commit `internal/api/testdata/golden/storage-graph-application-root-cytoscape.json`. Verify the two existing storage goldens are byte-identical (`git diff --exit-code` on them) and the new one carries the claimless pod with its `application` group and no edge.
- [x] 6.3 Add to `internal/api/storage_graph_test.go`: `TestStorageGraph_ApplicationRootIssuesRecovery` (a `?application=checkout` request captures a tracking-id-restricted `kube_deployment_annotations` query carrying `<az>`,`<env>` and the fixed `!=""`), `TestStorageGraph_ApplicationRootSuppressesNamespaceDerivation` (`pod=shop/x&application=y` ⇒ no `namespace` matcher), and extend `TestStorageGraph_EmbedderAndServerAgree` with an `application=` value. Verify all pass.

## 7. Documentation

- [x] 7.1 `docs/upstream-metrics.md`: in "Fan-out per `/v1/storage-graph` request" add the recovery wave (three stages, gating, per-family modes, the bound and its fallback, the tally rule), the D10 narrowing of the pod scope and its extra prerequisite, and the design D11 rows; note in "Which request filter reaches which series" that the recovery carries the same request matchers as the by-reference reads. Verify the stated counts match task 5.9.
- [x] 7.2 `docs/BREAKING.md`: add a non-breaking HTTP entry "`/v1/storage-graph` accepts an `application=` root" (what it matches, what it materialises, that a request without it is byte-identical) and, under the in-process-embedder heading, the `NewStorageScope` signature move and the new `StorageRoots.Applications` field. Verify it names the spec requirements it modifies.
- [x] 7.3 `README.md` and `README.zh-tw.md`: add `application=` to the storage-graph root list and one sentence on the recovery read (pods of the Application are loaded even when they mount nothing). Verify both READMEs describe the same behaviour.
- [x] 7.4 `CLAUDE.md`: update the request-surface bullet (roots list gains `application=<argo-app>`), the `/v1/storage-graph` lifecycle block (the recovery wave and its stages; `readScopedPods` waits on bindings AND the recovery), the storage-join bullet's test list (`appscope_test.go`, the new promql tests), and the "Pod-only roots" wording where it lists the roots that suppress derivation. Verify the totals agree with 5.9 and 7.1.
- [x] 7.5 Update the `--netapp-qos-scope-batch-bytes` help text in `internal/config/config.go` and `Options.QoSScopeBatchBytes` in `pkg/build/options.go` to name the application recovery's tracking-id and owner-name alternations. Verify `go vet ./internal/config/ ./pkg/build/` passes.

## 8. Verification gate

- [x] 8.1 Run `gofmt -l .`, `make vet`, `make lint`, `make check-docs`, `make check-parser-containment`, `make check-route-containment`; verify all clean.
- [x] 8.2 Run `make test` (`go test ./... -count=1 -race -shuffle=on`); verify green, and that `git status` shows no golden change except the new `storage-graph-application-root-cytoscape.json`.
- [x] 8.3 Run `go test ./internal/integration/ -run 'TestStorage' -v` with Docker available (or confirm CI's integration job) after adding `TestStorageGraphApplicationRoot` to `internal/integration` — ingest a Deployment-managed claimless pod and a mounting pod under one tracking-id, request `?application=`, assert both pods present, the claimless one edgeless, and a `?application=typo` body empty. Verify the test passes end to end against the VictoriaMetrics container.
- [x] 8.4 Run `openspec validate add-storage-graph-application-root --strict`; verify the change validates and every scenario in the two delta specs maps to at least one test named in groups 2–8.
