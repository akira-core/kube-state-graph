## 1. Parity pins (write first — they define "output-preserving")

- [x] 1.1 Add `TestBuildStorage_PlanIsOutputPreserving` to `pkg/build/build_storage_test.go`: one fixture set holding every family (pods with and without claims, a same-named claimless pod in another namespace, a claimless `pod=` root, services, endpointslices, container info); build once through today's full read and once through the storage plan (design D2/D3), serialise both bodies, strip `containers` from the first, and assert byte equality. Verify it fails before group 3 lands (the plan does not exist) and passes after.
- [x] 1.2 Add `TestParseStorageValues_DerivedNamespaceIsOutputPreserving` to the `pkg/kubegraph` storage-parse test file: for a pod-only-root request, build and project once with the derived selector and once with the selector's `Namespace` cleared against the same `MockQuerier` fixture, and assert byte-identical bodies (spec scenario "Pod roots push their namespaces upstream"). Verify the derived run issued `namespace=~"platform|shop"` on `kube_pod_info` and the undived run issued no `namespace` matcher.
- [x] 1.3 Run `go test ./internal/api/ -run TestGolden` and record that `internal/api/testdata/golden/storage-graph-*.json` carry no `containers` key (`grep -c containers` returns 0 for both); this is what makes the storage goldens a byte-level pin for the rest of the change.

## 2. `kube_pod_container_info` degrades on a query error

- [x] 2.1 Change the `kube_pod_container_info` leg in `pkg/build/topology.go` from `g.Go(fetch(...))` to `g.Go(fetchOptional(...))` (design D1); verify `grep -c "fetch(promql" pkg/build/topology.go` drops by one and the `fetchOptional(` count rises by one, total legs unchanged.
- [x] 2.2 Add `TestReadTopology_ContainerInfoLegDegrades` to `pkg/build/netapp_test.go` beside `TestReadTopology_AccumulatingAnnotationLegDegrades`: a `MockQuerier` erroring on `kube_pod_container_info` only; assert `ReadTopology` returns no error, no pod carries `Containers()`, and every other attribute matches the all-succeed run (spec scenario "Container info query error degrades"). Verify the test fails against the current `fetch` wiring.
- [x] 2.3 Extend `TestReadTopology_DegradingLegHonoursCallerCancellation` with a `kube_pod_container_info` case: an already-cancelled caller context plus the leg erroring returns an error (spec scenario "Caller cancellation still fails the container leg"). Verify the live-caller positive control pins the degrade: putting the leg back on `fetch` must make that half fail (mutation-checked).
- [x] 2.4 Update the `RawSeriesCount` doc comment in `pkg/build/topology.go` so the "0 also covers a degraded leg" sentence names three legs, and update the comment block above the annotation legs' `fetchOptional` calls to include the container leg's rationale (cardinality multiplies with live objects, image variants and churn). Verify `go vet ./pkg/build/` is clean.

## 3. Endpoint read plan

- [x] 3.1 Introduce `topologyPlan` (`skip map[promql.Query]bool`, `podScope *podScopeSpec`) and `fullPlan` / `storagePlan(roots graph.StorageRoots)` in `pkg/build/topology.go` (design D2); rename the body of `ReadTopology` to `readTopology(..., plan topologyPlan)` and keep `ReadTopology` as `readTopology(..., fullPlan)`. Verify `go build ./...` succeeds with no caller change and `TestReadTopology_FanOutLegCount` still passes at 37 / 43.
- [x] 3.2 Replace the literal `RawSeriesCount` map and the individual `g.Go(...)` calls for the first-wave legs with ONE table of `(query, *model.Vector, fetch kind)` entries iterated for both launch and tally, so a leg in `plan.skip` is neither launched nor tallied (its key absent, never 0). Verify `TestReadTopology_FanOutLegCount` passes unchanged and a new subtest asserts `RawSeriesCount` has no key for a skipped query.
- [x] 3.3 Make `storagePlan` skip `QPodContainerInfo`, `QServiceInfo`, `QEndpointSliceEndpoints`, `QEndpointSliceLabels`, `QServiceAnnotations`, and switch `Builder.BuildStorage` to `readTopology(..., storagePlan(roots))` (roots wiring lands in 4.6; pass `graph.StorageRoots{}` until then). Verify `TestBuildStorage_IssuesNoServiceGraphOrProbeQuery` is extended to assert none of the five queries is issued (spec scenario "Skipped families never reach the upstream") and passes.
- [x] 3.4 Add `TestBuildStorage_FanOutLegCount` to `pkg/build/build_storage_test.go` pinning 32 legs with a one-chunk pod scope and no matched volume, 38 with one, and 30 / 36 with an empty pod scope (design D11); verify each of the four counts fails when one skipped leg is re-added to the storage plan.
- [x] 3.5 Run the parity pin 1.1; verify it passes with `containers` stripped and fails if the strip step is removed (proving the ONE observable difference is exactly `containers`).

## 4. Scoped pod read (`kube_pod_info`, `kube_pod_owner` by reference)

- [x] 4.1 Add `promql.RenderScoped(q, window, keys, sel, values) (string, bool)` in `pkg/promql/scope.go` composing fixed selector → request matchers → QuoteMeta alternation, gated on a hardcoded table `{QPodInfo: "pod", QPodOwner: "pod"}` naming each query's scope label (design D4); share `normaliseValues` and the alternation helper with `RenderQoSVolumeScoped`. Verify `TestRenderScoped` covers: empty values ⇒ `ok=false`; non-scopeable query ⇒ `ok=false`; a metacharacter in a pod name matches itself only; the empty selector renders `last_over_time(kube_pod_info{pod=~"a|b"}[5m])`; a selector with az/env/namespace renders those matchers BEFORE the `pod` alternation.
- [x] 4.2 Generalise `ChunkQoSVolumeScope` to `ChunkScope(values []string, budget int) [][]string` and keep `ChunkQoSVolumeScope` as a one-line alias; verify `TestChunkQoSVolumeScope` passes unchanged and `TestRender_EmptySelectorMatchesBaseline` / `render-baseline.txt` are byte-identical (`RenderScoped` is not a `Render` case).
- [x] 4.3 Pin in `pkg/promql/scope_test.go` (beside, not inside, `TestRender_ComposesFixedSelectorFirst`, since `RenderScoped` is not a `Render` case) that for `kube_pod_info` and `kube_pod_owner` the scoped output is `Render`'s with exactly the alternation appended after the request matchers, and add a `TestQueryDims_ScopedPodLegsUnchanged` asserting both queries still map to `dimsNamespaced`. Verify both pass and that removing a query from the scopeable set fails the first.
- [x] 4.4 Create `pkg/build/podscope.go` with `readScopedPods(ctx, callerCtx, q, window, end, opts, sel, plan, v, bindingsDone)` mirroring `readScopedQoS` (design D5): wait on the `QPVCBindings` done-channel alone; scope = sorted, de-duplicated union of `v.PVC`'s `pod` labels and `plan.podScope.roots` names; empty scope ⇒ return with both vectors nil and no query; chunk under `opts.qosScopeBatchBytes()`; issue `2 × chunks` queries under `qosScopeConcurrency` (rename the constant to `scopeConcurrency`) under the bare family names; merge each family in chunk-index order; return the FIRST chunk error into the errgroup (fail closed). Verify `go build ./...` and `go vet ./pkg/build/` are clean.
- [x] 4.5 Wire the wave into `readTopology`: when `plan.podScope != nil`, launch `signalWhenDone(fetch(QPVCBindings, &v.PVC), bindingsDone)` and `readScopedPods` instead of the two unscoped `fetch` calls, and tally both families from the merged vectors only when a query was issued. Verify `TestBuildStorage_FanOutLegCount` (3.4) passes at all four counts.
- [x] 4.6 Change `Builder.BuildStorage` and `Engine.BuildStorage` to take `roots graph.StorageRoots` (design D7); pass `req.Scope.Roots` from `Engine.BuildStorageFromValues` and from `handleStorageGraph` in `internal/api/handlers.go` (the `runBuild` argument becomes a closure capturing the roots). Verify `go build ./...` and `go vet ./...` succeed and `TestStorageGraph_EmbedderAndServerAgree` passes.
- [x] 4.7 Add to `pkg/build/podscope_test.go`: `TestReadScopedPods_RestrictedToMountingPodsAndRoots` (queries carry exactly the expected `pod` alternation plus the request's az/env matchers; spec scenario "Pod read is restricted to mounting pods and roots"), `TestReadScopedPods_EmptyScopeIssuesNothing` (no `kube_pod_info` / `kube_pod_owner` query; tally has neither key), `TestReadScopedPods_ChunksMergeInChunkOrder` (two chunks answered in reverse order yield the same vector order), `TestReadScopedPods_ChunkFailureFailsBuild` (second of three chunks errors ⇒ `ReadTopology` error; spec scenario "A pod chunk failure fails the build"), `TestReadScopedPods_ClaimlessRootIsLoaded` (spec scenario "Claimless pod root is still drawn"). Verify all five pass and the failure test fails if the chunk error is swallowed.
- [x] 4.8 Add `TestBuildStorage_CrossNamespaceNameCollisionIsHarmless` to `pkg/build/build_storage_test.go`: bindings name `shop/web-0`, fixture also returns `platform/web-0` for the scoped query; assert the projected body omits `platform/web-0` and equals the unscoped body (spec scenario "Cross-namespace name collision is harmless"). Verify it passes.
- [x] 4.9 Confirm `TestGolden_StorageGraphResponses` needs no fixture change (its estate is hand-built, never read through a querier) and update `internal/api/storage_graph_test.go` for the storage plan (no `kube_pod_info` with an empty scope; none of the five skipped families); run `go test ./internal/api/ -run 'TestGolden_StorageGraphResponses|TestStorageGraph'` WITHOUT `-update` and verify `git status` shows no change under `internal/api/testdata/golden/`.

## 5. Pod-only roots derive the namespace selector

- [x] 5.1 Add pure `deriveStorageNamespaces(scope graph.StorageScope, explicit []string) []string` to `pkg/kubegraph/parse.go` (design D8): returns `explicit` when it has a non-empty value, or when `scope.Roots.RequestedStorage()` is true, or when `scope.Roots.Nodes` is non-empty, or when `scope.Roots.Pods` is empty; otherwise the sorted, de-duplicated `PodRef.Namespace` set. Wire it into `ParseStorageValues` as `req.Selector.Namespace`, leaving `req.Scope.Namespaces` as parsed. Verify `go build ./...` succeeds.
- [x] 5.2 Add table-driven `TestParseStorageValues_DerivesNamespaceFromPodOnlyRoots`: `?pod=shop/orders-0&pod=platform/redis-0` ⇒ `Namespace=[platform shop]`; `?pod=shop/orders-0&aggr=aggr1` ⇒ none; `?pod=shop/orders-0&node=n1` ⇒ none; `?pod=shop/orders-0&namespace=platform` ⇒ `[platform]`; `?pod=shop/x&namespace=` (empty value) ⇒ `[shop]`; `?pod=b/x&pod=a/y` and `?pod=a/y&pod=b/x` ⇒ identical selector (spec scenarios "Mixed roots do not derive a namespace", "Explicit namespace wins over roots", "Root order does not change the queries"). Verify all cases pass.
- [x] 5.3 Extend `TestStorageGraph_SelectorQueriesCaptured` in `internal/api/storage_graph_test.go` with a pod-only-root request; assert `kube_pod_info` carries `namespace=~"platform|shop"` and `ALERTS` carries `namespace=~"platform|shop|"` (spec scenario "Pod roots push their namespaces upstream"). Verify it passes and fails when 5.1 is reverted.
- [x] 5.4 Run the parity pin 1.2; verify byte equality holds.

## 6. Result-cardinality observability

- [x] 6.1 Add `promql.SeriesMetrics` (`ObserveQuerySeries(name string, n int)`) and `seriesMetricsOf` beside `RouterMetrics` / `routerMetricsOf` in `pkg/promql` (design D9); call it in `Client.Instant` after the successful `model.Vector` assertion with `len(vec)`, and NOT on any error path. Verify a unit test with a fake implementing both `Metrics` and `SeriesMetrics` records one observation per successful query and none for a failed one, and that a `Metrics`-only fake still compiles and works (the upgrade is optional).
- [x] 6.2 Add `UpstreamQuerySeries *prometheus.HistogramVec` (`kube_state_graph_upstream_query_result_series`, `Buckets: prometheus.ExponentialBuckets(1024, 2, 11)`, label `query`) to `internal/observability/metrics.go`, register it, and add the `ObserveQuerySeries` adapter in `adapters.go`. Verify `TestMetrics_ResultSeriesHistogramExposed` (`internal/api/metrics_backend_test.go`) scrapes the metric with `le="65536"` / `le="131072"` buckets, and that no series carries a `backend` label (spec scenario "Result-series histogram after a build").
- [x] 6.3 Add `largestLeg(counts map[string]int) (string, int)` (lexical tie-break) in `pkg/build`, and add `largest_leg` / `largest_leg_series` to the `graph built` and `storage graph built` Info logs plus `raw_series_counts` at Debug on both paths (design D10). Verify a unit test pins the tie-break and `go vet ./pkg/build/` is clean.
- [x] 6.4 Confirm a chunked pod scope is issued once per chunk under the bare family name (`TestReadScopedPods_ChunksMergeInChunkOrder`), so `Client.Instant` — which observes once per successful call (`TestInstant_ObservesResultSeries`) — records one observation per chunk; the build layer talks to a `Querier`, never to `SeriesMetrics`, so the fan-out pins need no metrics fake. Verify both tests pass.

## 7. Documentation

- [x] 7.1 `docs/upstream-metrics.md`: update the query-error matrix from "20 kube-state-metrics topology queries fail the build / 2 degrade" to 19 / 3 naming `kube_pod_container_info` and its multiplying-cardinality criterion; update the `/v1/graph` fan-out diagram counts accordingly; add a "Fan-out per `/v1/storage-graph` request" section showing the five skipped families, the scoped pod wave (waits on the binding leg, empty scope issues nothing, chunked, fail-closed) and the 32 / 38 and 30 / 36 pins; note that `--netapp-qos-scope-batch-bytes` bounds every data-derived alternation. Verify the matrix rows still sum to the full-plan leg count and the pin numbers match 3.4.
- [x] 7.2 `docs/BREAKING.md`: add an entry covering (a) `/v1/graph` returning 200 with `containers` absent on a `kube_pod_container_info` query error, with the failures-counter alerting guidance; (b) `/v1/storage-graph` pod nodes no longer carrying `data.containers`; (c) the `Builder.BuildStorage` / `Engine.BuildStorage` signature gaining `graph.StorageRoots`; (d) pod-only-root requests rendering a derived `namespace` matcher (queries differ, bodies do not). Verify each item names the spec requirement it changes.
- [x] 7.3 `docs/upstream-backend-routing.md` metric table and the `graph-api` self-metric list in `README.md` / `README.zh-tw.md` (if present): add `kube_state_graph_upstream_query_result_series` with its label set and bucket range and the "one observation per issued query / chunk / backend" rule. Verify the metric name matches `metrics.go` byte-for-byte.
- [x] 7.4 `README.md` and `README.zh-tw.md`: update the `kube_pod_container_info` row's "Required?" / failure-mode wording to log-and-continue, and add one sentence to the storage-graph section stating the body carries no `containers` and reads pods by reference. Verify both READMEs state the same counts and neither still says the storage build reuses the full fan-out.
- [x] 7.5 `CLAUDE.md`: update the request-lifecycle bullet (`ReadTopology` … `20 fetch + 17 fetchOptional` ⇒ `19 + 18`; storage build issues 32 / 38 or 30 / 36 through the storage plan and skips the five families), the `/v1/storage-graph` lifecycle block (`BuildStorage` takes roots; scoped pod wave), the storage-join bullet's test list (add `podscope_test.go`), and the self-metrics decision bullet (new histogram via the `SeriesMetrics` upgrade). Verify the stated totals agree with 3.4 and 7.1.
- [x] 7.6 Update the flag help text for `--netapp-qos-scope-batch-bytes` in `internal/config/config.go` to say it bounds every data-derived alternation (QoS `volume` and storage `pod`), and the `Options.QoSScopeBatchBytes` doc comment likewise. Verify `make check-docs` is clean (no OpenAPI change) and `go vet ./internal/config/` passes.

## 8. Verification gate

- [x] 8.1 Run `make vet` and `make lint`; verify both report clean (including `make check-parser-containment` / `make check-route-containment` if wired into lint).
- [x] 8.2 Run `make test`; verify the full unit / component / golden / property suite passes with `-race -shuffle=on` and `git status` shows no golden file changed.
- [x] 8.3 Run `go test ./internal/integration/ -run 'TestGraphSuite/TestStorageGraph|TestGraphSuite'` with Docker available; verify the storage suite passes against real VictoriaMetrics, including a claimless `pod=` root being drawn and a pod-only-root request returning the same body as the same request with an explicit matching `namespace=`.
- [x] 8.4 Run `make check-docs`; verify no OpenAPI drift (`docs/swagger.{json,yaml}` regenerate byte-identical; the target diffs all of `docs/`, so it passes only once this change's markdown edits are committed).
- [x] 8.5 Run `openspec validate "harden-topology-read-cardinality"`; verify the change is valid with no outstanding artifact issues.
